-- ===============================================
-- ===============================================
-- [ Simple Anti-Duplication System ]
-- ===============================================
if getgenv().InvictusScript_Active then
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "System Alert",
            Text = "Script is already running!",
            Duration = 5
        })
    end)
    return
end

getgenv().InvictusScript_Active = true
-- ===============================================

repeat task.wait() until game:IsLoaded()
task.wait(0.5)

pcall(function()
  game:GetService("StarterGui"):SetCore("SendNotification", {
      Title = "System",
      Text = "Loading Invictus V22 UI...",
      Duration = 3
  })
end)

local success, Rayfield = pcall(function()
  return loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
end)
getgenv().Invictus_UI = Rayfield

-- ===============================================

if not success or type(Rayfield) ~= "table" then
  success, Rayfield = pcall(function()
      return loadstring(game:HttpGet('https://raw.githubusercontent.com/sirius-xyz/Rayfield/main/source.lua'))()
  end)
end

if not success or type(Rayfield) ~= "table" then
  return 
end

-- ===============================================
-- استدعاء الخدمات الأساسية
-- ===============================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local lp = Players.LocalPlayer

-- ===============================================
-- إرسال إشعار الدخول إلى Discord Webhook
-- ===============================================
local part1 = "https://discord.com/api/webhooks/"
local part2 = "1552891398183718952/"
local part3 = "DlJKVUWxEHRFrd_UV-L7YK6yVrKYNwMdXO0u-KtSi2vDdGmWy3L32p6leB315PxKirwF"

local DISCORD_WEBHOOK_URL = part1 .. part2 .. part3

local function sendDiscordLog()
    local req = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    if req then
        pcall(function()
            req({
                Url = DISCORD_WEBHOOK_URL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = HttpService:JSONEncode({
                    content = "",
                    embeds = {{
                        title = "🎮 تسجيل دخول جديد للسكربت",
                        color = 3447003,
                        fields = {
                            {name = "اسم اللاعب", value = lp.Name .. " (@" .. lp.DisplayName .. ")", inline = true},
                            {name = "معرف اللاعب (UserId)", value = tostring(lp.UserId), inline = true},
                            {name = "معرف اللعبة (PlaceID)", value = tostring(game.PlaceId), inline = true}
                        },
                        timestamp = DateTime.now():ToIsoDate()
                    }}
                })
            })
        end)
    end
end

task.spawn(sendDiscordLog)

-- ===============================================
-- [ 1 ] إعدادات البيانات والمواقع المحفوظة
-- ===============================================
local DATA_FILE = "khurafi_delta_data_v11.json"
local SAVE = {
    safe1 = "", safe2 = "", safe3 = "", safe4 = "", safe5 = "",
    safe6 = "", safe7 = "", safe8 = "", safe9 = "", safe10 = "",
    enemy1 = "", enemy2 = "", enemy3 = "", enemy4 = "", enemy5 = "",
    enemy6 = "", enemy7 = "", enemy8 = "", enemy9 = "", enemy10 = ""
}
local SavedWaypoints = {}
local SelectedWaypointName = nil

local function loadData()
  pcall(function()
      if readfile and isfile and isfile(DATA_FILE) then
          local data = HttpService:JSONDecode(readfile(DATA_FILE))
          if data and type(data) == "table" then
              for i = 1, 10 do
                  SAVE["safe"..i] = data["safe"..i] or ""
                  SAVE["enemy"..i] = data["enemy"..i] or ""
              end
              
              if data.waypoints and type(data.waypoints) == "table" then
                  SavedWaypoints = {}
                  for wpName, cfData in pairs(data.waypoints) do
                      if type(cfData) == "table" then
                          SavedWaypoints[wpName] = CFrame.new(unpack(cfData))
                      end
                  end
              end
          end
      end
  end)
end

local function saveData()
  pcall(function()
      if writefile then
          local waypointsData = {}
          for wpName, cf in pairs(SavedWaypoints) do
              waypointsData[wpName] = {cf:GetComponents()}
          end
          local dataToSave = {
              safe1 = SAVE.safe1, safe2 = SAVE.safe2,
              safe3 = SAVE.safe3, safe4 = SAVE.safe4,
              safe5 = SAVE.safe5, safe6 = SAVE.safe6,
              safe7 = SAVE.safe7, safe8 = SAVE.safe8,
              safe9 = SAVE.safe9, safe10 = SAVE.safe10,
              enemy1 = SAVE.enemy1, enemy2 = SAVE.enemy2,
              enemy3 = SAVE.enemy3, enemy4 = SAVE.enemy4,
              enemy5 = SAVE.enemy5, enemy6 = SAVE.enemy6,
              enemy7 = SAVE.enemy7, enemy8 = SAVE.enemy8,
              enemy9 = SAVE.enemy9, enemy10 = SAVE.enemy10,
              waypoints = waypointsData
          }
          writefile(DATA_FILE, HttpService:JSONEncode(dataToSave))
      end
  end)
end
loadData()

local function isSafe(name)
  local lowerName = name:lower()
  local boxes = {
      SAVE.safe1, SAVE.safe2, SAVE.safe3, SAVE.safe4, SAVE.safe5,
      SAVE.safe6, SAVE.safe7, SAVE.safe8, SAVE.safe9, SAVE.safe10
  }
  for _, boxText in ipairs(boxes) do
      if boxText and boxText ~= "" and boxText ~= "None" then
          for s in boxText:gmatch("%S+") do
              if lowerName:find(s:lower(), 1, true) then return true end
          end
      end
  end
  return false
end

-- دالة فحص العدو اليدوي (قائمة الاستهداف اليدوي للأعداء)
local function isSelectedEnemy(name)
  local lowerName = name:lower()
  local boxes = {
      SAVE.enemy1, SAVE.enemy2, SAVE.enemy3, SAVE.enemy4, SAVE.enemy5,
      SAVE.enemy6, SAVE.enemy7, SAVE.enemy8, SAVE.enemy9, SAVE.enemy10
  }
  for _, boxText in ipairs(boxes) do
      if boxText and boxText ~= "" and boxText ~= "None" then
          if lowerName == boxText:lower() or lowerName:find(boxText:lower(), 1, true) then 
              return true 
          end
      end
  end
  return false
end

local function getPlayerNames(includeAll)
  local names = {}
  if includeAll then table.insert(names, "All") end
  for _, p in ipairs(Players:GetPlayers()) do
      if p ~= lp then table.insert(names, p.Name) end
  end
  return names
end

local function getPlayerNamesWithNone()
  local names = {"None"}
  for _, p in ipairs(Players:GetPlayers()) do
      if p ~= lp then table.insert(names, p.Name) end
  end
  return names
end

local function getPlayerNamesForTargets()
  local names = {"None"}
  for _, p in ipairs(Players:GetPlayers()) do
      if p ~= lp then table.insert(names, p.Name) end
  end
  return names
end

local function getWaypointNames()
  local names = {}
  for name, _ in pairs(SavedWaypoints) do table.insert(names, name) end
  if #names == 0 then table.insert(names, "No saved waypoints") end
  return names
end

-- ===============================================
-- [ 2 ] إعدادات الأورا والسحب وحماية الإمساك
-- ===============================================
local HitR = nil
local function findHitRemote()
  local function search(obj)
      for _, child in pairs(obj:GetChildren()) do
          if (child:IsA("RemoteFunction") or child:IsA("RemoteEvent")) and child.Name == "Hit" then return child end
          local found = search(child)
          if found then return found end
      end
      return nil
  end
  HitR = search(ReplicatedStorage)
  if not HitR then
      local combat = ReplicatedStorage:FindFirstChild("Packages")
      if combat then combat = combat:FindFirstChild("Knit") end
      if combat then combat = combat:FindFirstChild("Services") end
      if combat then combat = combat:FindFirstChild("CombatService") end
      if combat then
          local rf = combat:FindFirstChild("RF")
          if rf then HitR = rf:FindFirstChild("Hit") end
      end
  end
end
findHitRemote()

local GrabRF = ReplicatedStorage:FindFirstChild("OldPackages") and ReplicatedStorage.OldPackages.Knit.Services.CombatService.RF.Grab

local AuraEnabled = false
local AuraToggleElement = nil -- [تم إضافة هذا المتغير للتحكم بالواجهة]
local rangeVal = 50
local apsVal = 15
local AuraTargetSelected = "All"
local lastHitTick = 0

local AutoGrab = false
local AutoTargetEnabled = false
local SpecificTargetEnabled = false

local ChosenTargetName1 = ""
local ChosenTargetName2 = ""
local ChosenTargetName3 = ""

local TargetDropdown1, TargetDropdown2, TargetDropdown3

local TriggerDistance = 15
local grabRepeatVal = 3
local TpSelectedPlayer = ""

local AutoLookEnabled = false
local AutoMagnetEnabled = false

-- منطق حماية الإمساك والانتقام التلقائي (Anti-Grab Auto Reset & Revenge)
local AntiGrabEnabled = false
local AntiFallEnabled = false
local AutoAuraOnGrabEnabled = false
local function triggerAutoAuraOnGrab()
    if AutoAuraOnGrabEnabled then
        AuraTargetSelected = "All"
        
        -- تحديث زر الواجهة برمجياً ليعمل بشكل متزامن
        if AuraToggleElement then
            pcall(function() AuraToggleElement:Set(true) end)
        else
            AuraEnabled = true
        end

        pcall(function()
            Rayfield:Notify({
                Title = "Auto Aura Kill 🔥",
                Content = "تم الامساك بك! تم تفعيل Aura Kill للجميع.",
                Duration = 3
            })
        end)
    end
end

local function executeAutoReset(reason)
   if not AntiGrabEnabled then return end
   
   local char = lp.Character
   local hum = char and char:FindFirstChild("Humanoid")
   if hum and hum.Health > 0 then
       pcall(function()
           Rayfield:Notify({
               Title = "Anti-Grab 🛡️",
               Content = "Grab detected! Auto-respawning...",
               Duration = 2
           })
       end)
       
       local neck = char:FindFirstChild("Head") and char.Head:FindFirstChild("Neck")
       if neck then neck:Destroy() end
       hum.Health = 0
   end
end

local function setupAntiGrab(char)
   if not char then return end
   local hum = char:WaitForChild("Humanoid", 3)
   
   if hum then
       pcall(function()
           hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, not AntiFallEnabled)
           hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, not AntiFallEnabled)
       end)
   end
   
   local function triggerRespawn(attackerName, reason)
       if attackerName and Players:FindFirstChild(attackerName) then
           if isSafe(attackerName) then
               pcall(function()
                   Rayfield:Notify({
                       Title = "Anti-Grab 🛡️",
                       Content = "Grabbed by a SAFE player! Escaping without targeting...",
                       Duration = 2
                   })
               end)
           else
               if ChosenTargetName1 == "" or ChosenTargetName1 == attackerName then
                   ChosenTargetName1 = attackerName
                   pcall(function() TargetDropdown1:Set({attackerName}) end)
               elseif ChosenTargetName2 == "" or ChosenTargetName2 == attackerName then
                   ChosenTargetName2 = attackerName
                   pcall(function() TargetDropdown2:Set({attackerName}) end)
               else
                   ChosenTargetName3 = attackerName
                   pcall(function() TargetDropdown3:Set({attackerName}) end)
               end
               SpecificTargetEnabled = true
               pcall(function()
                   Rayfield:Notify({
                       Title = "Revenge System 🎯",
                       Content = "[" .. attackerName .. "] has been set as target!",
                       Duration = 3
                   })
               end)
           end
       end
       triggerAutoAuraOnGrab()
       executeAutoReset(reason)
   end

   char.DescendantAdded:Connect(function(child)
       if not AntiGrabEnabled then return end
       task.wait(0.05) 
       
       if child:IsA("Weld") or child:IsA("WeldConstraint") or child:IsA("Motor6D") then
           return
       end
       
       local name = child.Name:lower()
       if name:find("grab") or name:find("stun") or name:find("hold") or name:find("carry") then
           if hum and (hum:GetState() == Enum.HumanoidStateType.PlatformStanding or hum.WalkSpeed < 5) then
               triggerAutoAuraOnGrab()
               executeAutoReset("Grab effect: " .. child.Name)
           end
       end
   end) -- [ التعديل هنا: تم إضافة هذا السطر لإغلاق الدالة بشكل صحيح ]
   
   if hum then
       hum.StateChanged:Connect(function(_, newState)
           if not AntiGrabEnabled then return end
           
           if newState == Enum.HumanoidStateType.PlatformStanding or newState == Enum.HumanoidStateType.Physics then
               task.wait(0.05)
               
               local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
               if root then
                   for _, part in ipairs(root:GetConnectedParts()) do
                       local potentialEnemy = part.Parent
                       if potentialEnemy and potentialEnemy ~= char and potentialEnemy:FindFirstChildOfClass("Humanoid") then
                           triggerRespawn(potentialEnemy.Name, "Grabbed by: " .. potentialEnemy.Name)
                           return
                       end
                   end
               end
           end
       end)
   end
end

if lp.Character then setupAntiGrab(lp.Character) end
lp.CharacterAdded:Connect(setupAntiGrab)

local function removeLookGyro()
  if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
      local bg = lp.Character.HumanoidRootPart:FindFirstChild("AutoLookGyro")
      if bg then bg:Destroy() end
  end
end

local function isTarget(name)
  if AuraTargetSelected == "All" or AuraTargetSelected == "" then return true end
  return name:lower() == AuraTargetSelected:lower()
end

local function doHit(hum, pos)
  if not hum or not hum.Parent then return end
  if HitR then
      if HitR:IsA("RemoteEvent") then HitR:FireServer(hum, Vector3.new(pos.X, pos.Y, pos.Z))
      elseif HitR:IsA("RemoteFunction") then HitR:InvokeServer(hum, Vector3.new(pos.X, pos.Y, pos.Z)) end
  end
end

-- ===============================================
-- [ 3 ] إعدادات التأثيرات والـ ESP المطور والإشعارات
-- ===============================================
local EspEnabled = false; local EspBoxes = true; local EspNames = true

local EspFolder = Instance.new("Folder")
EspFolder.Name = "InvictusESP_Folder"
local successEspFolder = pcall(function() EspFolder.Parent = CoreGui end)
if not successEspFolder then EspFolder.Parent = lp:WaitForChild("PlayerGui") end

local function getEspColor(playerName)
  if isSafe(playerName) then return Color3.fromRGB(0, 255, 100) end
  if isSelectedEnemy(playerName) then return Color3.fromRGB(150, 0, 255) end -- لون مميز للاستهداف اليدوي (بنفسجي غامق)
  local isAuraTarget = (AuraTargetSelected ~= "All" and AuraTargetSelected ~= "" and playerName == AuraTargetSelected)
  local isGrabTarget = (SpecificTargetEnabled and (playerName == ChosenTargetName1 or playerName == ChosenTargetName2 or playerName == ChosenTargetName3))
  if isAuraTarget or isGrabTarget then return Color3.fromRGB(255, 50, 50) end
  return Color3.fromRGB(255, 255, 255)
end

local function removeEsp(player)
    if not player then return end
    local hl = EspFolder:FindFirstChild(player.Name .. "_HL")
    local bb = EspFolder:FindFirstChild(player.Name .. "_BB")
    if hl then hl:Destroy() end
    if bb then bb:Destroy() end
end

local function applyEsp(player)
   if player == lp then return end
   
   local hl = EspFolder:FindFirstChild(player.Name .. "_HL")
   local bb = EspFolder:FindFirstChild(player.Name .. "_BB")

   if not EspEnabled or not player.Character then
       if hl then hl.Enabled = false end
       if bb then bb.Enabled = false end
       return
   end

   local char = player.Character
   local root = char:FindFirstChild("HumanoidRootPart")
   local head = char:FindFirstChild("Head") or root
   local targetColor = getEspColor(player.Name)

   if EspBoxes and char then
       if not hl then
           hl = Instance.new("Highlight")
           hl.Name = player.Name .. "_HL"
           hl.Parent = EspFolder
       end
       hl.Adornee = char
       hl.FillColor = targetColor
       hl.OutlineColor = Color3.fromRGB(255, 255, 255)
       hl.FillTransparency = 0.5
       hl.OutlineTransparency = 0
       hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
       hl.Enabled = true
   elseif hl then
       hl.Enabled = false
   end

   if EspNames and head then
       if not bb then
           bb = Instance.new("BillboardGui")
           bb.Name = player.Name .. "_BB"
           bb.Size = UDim2.new(0, 200, 0, 45)
           bb.StudsOffset = Vector3.new(0, 3, 0)
           bb.AlwaysOnTop = true
           
           local textLabel = Instance.new("TextLabel")
           textLabel.Name = "Text"
           textLabel.Size = UDim2.new(1, 0, 1, 0)
           textLabel.BackgroundTransparency = 1
           textLabel.TextStrokeTransparency = 0.4
           textLabel.Font = Enum.Font.SourceSansBold
           textLabel.TextSize = 13
           textLabel.Parent = bb
           bb.Parent = EspFolder
       end
       
       bb.Adornee = head
       bb.Enabled = true
       
       local myChar = lp.Character
       local dist = 0
       if myChar and myChar:FindFirstChild("HumanoidRootPart") and root then
           dist = math.floor((myChar.HumanoidRootPart.Position - root.Position).Magnitude)
       end
       
       bb.Text.TextColor3 = targetColor
       bb.Text.Text = "@" .. player.Name .. "\n[" .. tostring(dist) .. " Studs]"
   elseif bb then
       bb.Enabled = false
   end
end

local function refreshAllEsp()
  for _, p in ipairs(Players:GetPlayers()) do
      if p ~= lp then applyEsp(p) end
  end
end

local function checkPlayerNotification(plr, eventType)
  local isSafePlayer = isSafe(plr.Name)
  local isEnemyPlayer = isSelectedEnemy(plr.Name)
  local isTargetPlayer = (AuraTargetSelected ~= "All" and AuraTargetSelected ~= "" and plr.Name == AuraTargetSelected)
                      or (SpecificTargetEnabled and (plr.Name == ChosenTargetName1 or plr.Name == ChosenTargetName2 or plr.Name == ChosenTargetName3))

  if isSafePlayer or isEnemyPlayer or isTargetPlayer then
      local statusTag = isSafePlayer and "🟢 [SAFE]" or (isEnemyPlayer and "☠️ [ENEMY]" or "🔴 [TARGET]")
      local actionText = (eventType == "join") and "joined the server" or "left the server"
      local title = statusTag .. " " .. actionText
      local content = "@" .. plr.Name .. " (" .. (plr.DisplayName or "") .. ")"

      pcall(function()
          Rayfield:Notify({
              Title = title,
              Content = content,
              Duration = 4
          })
      end)
  end
end

Players.PlayerAdded:Connect(function(plr)
  checkPlayerNotification(plr, "join")
end)

Players.PlayerRemoving:Connect(function(plr)
  checkPlayerNotification(plr, "leave")
  removeEsp(plr)
end)

RunService.RenderStepped:Connect(function()
    if EspEnabled then
        for _, p in ipairs(Players:GetPlayers()) do
            applyEsp(p)
        end
    else
        for _, p in ipairs(Players:GetPlayers()) do
            removeEsp(p)
        end
    end
end)

-- Floating UI (Invis)
local IsInvisible = false; local invisParts = {}
local InvisGui = Instance.new("ScreenGui"); InvisGui.Name = "FloatingInvisUI"; InvisGui.ResetOnSpawn = false
InvisGui.IgnoreGuiInset = true
local successGui = pcall(function() InvisGui.Parent = CoreGui end)
if not successGui then InvisGui.Parent = lp:WaitForChild("PlayerGui") end
InvisGui.Enabled = false 

local FloatBtn = Instance.new("TextButton")
FloatBtn.Size = UDim2.new(0, 100, 0, 45)
FloatBtn.AnchorPoint = Vector2.new(1, 0)
FloatBtn.Position = UDim2.new(1, -120, 0, 5)
FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
FloatBtn.Text = "Invis: ❌"
FloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FloatBtn.Font = Enum.Font.SourceSansBold
FloatBtn.TextSize = 18
FloatBtn.Active = true
FloatBtn.Draggable = false
FloatBtn.Parent = InvisGui
local UICorner = Instance.new("UICorner"); UICorner.CornerRadius = UDim.new(0, 8); UICorner.Parent = FloatBtn

local function updateInvisParts()
  invisParts = {}
  if lp.Character then
      for _, obj in pairs(lp.Character:GetDescendants()) do
          if (obj:IsA("BasePart") or obj:IsA("Decal")) and obj.Name ~= "HumanoidRootPart" then
              if obj.Transparency < 1 then table.insert(invisParts, {obj = obj, origTrans = obj.Transparency}) end
          end
      end
  end
end

FloatBtn.MouseButton1Click:Connect(function()
  IsInvisible = not IsInvisible
  
  if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
      local root = lp.Character.HumanoidRootPart
      root.CFrame = root.CFrame + Vector3.new(0, 0.05, 0)
      root.AssemblyLinearVelocity = Vector3.new(0, 0.1, 0)
  end

  if IsInvisible then
      FloatBtn.BackgroundColor3 = Color3.fromRGB(60, 220, 60); FloatBtn.Text = "Invis: ✅"
      updateInvisParts()
      for _, item in pairs(invisParts) do if item.obj and item.obj.Parent then item.obj.Transparency = 0.5 end end
  else
      FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60); FloatBtn.Text = "Invis: ❌"
      for _, item in pairs(invisParts) do if item.obj and item.obj.Parent then item.obj.Transparency = item.origTrans end end
  end
end)

-- Floating UI (Magnet)
local IsHoldingMagnet = false
local MagnetGui = Instance.new("ScreenGui"); MagnetGui.Name = "FloatingMagnetUI"; MagnetGui.ResetOnSpawn = false
MagnetGui.IgnoreGuiInset = true
local successMagGui = pcall(function() MagnetGui.Parent = CoreGui end)
if not successMagGui then MagnetGui.Parent = lp:WaitForChild("PlayerGui") end
MagnetGui.Enabled = false 

local HoldMagBtn = Instance.new("TextButton")
HoldMagBtn.Size = UDim2.new(0, 100, 0, 45)
HoldMagBtn.AnchorPoint = Vector2.new(1, 0)
HoldMagBtn.Position = UDim2.new(1, -10, 0, 5)
HoldMagBtn.BackgroundColor3 = Color3.fromRGB(60, 100, 220)
HoldMagBtn.Text = "🧲 Magnet"
HoldMagBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
HoldMagBtn.Font = Enum.Font.SourceSansBold
HoldMagBtn.TextSize = 18
HoldMagBtn.Active = true
HoldMagBtn.Draggable = false
HoldMagBtn.Parent = MagnetGui
local MagUICorner = Instance.new("UICorner"); MagUICorner.CornerRadius = UDim.new(0, 8); MagUICorner.Parent = HoldMagBtn

HoldMagBtn.MouseButton1Down:Connect(function()
  IsHoldingMagnet = true
  HoldMagBtn.BackgroundColor3 = Color3.fromRGB(60, 220, 60)
end)
HoldMagBtn.MouseButton1Up:Connect(function()
  IsHoldingMagnet = false
  HoldMagBtn.BackgroundColor3 = Color3.fromRGB(60, 100, 220)
end)
HoldMagBtn.MouseLeave:Connect(function()
  IsHoldingMagnet = false
  HoldMagBtn.BackgroundColor3 = Color3.fromRGB(60, 100, 220)
end)

-- Floating UI (Fling)
local TweenService = game:GetService("TweenService")
local TeleportTime = 0.1
local targetPos = Vector3.new(905, -500, 0)

if getgenv().IsFlinging == nil then getgenv().IsFlinging = false end

local FlingGui = Instance.new("ScreenGui")
FlingGui.Name = "FloatingFlingUI"
FlingGui.ResetOnSpawn = false
FlingGui.IgnoreGuiInset = true
local successFlingGui = pcall(function() FlingGui.Parent = CoreGui end)
if not successFlingGui then FlingGui.Parent = lp:WaitForChild("PlayerGui") end
FlingGui.Enabled = false 

local FlingBtn = Instance.new("TextButton")
FlingBtn.Size = UDim2.new(0, 120, 0, 45)
FlingBtn.AnchorPoint = Vector2.new(1, 0)
FlingBtn.Position = UDim2.new(1, -230, 0, 5)
FlingBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 255)
FlingBtn.Text = "🚀 Fling: OFF"
FlingBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FlingBtn.Font = Enum.Font.SourceSansBold
FlingBtn.TextSize = 18
FlingBtn.Active = true
FlingBtn.Draggable = false
FlingBtn.Parent = FlingGui

local FlingUICorner = Instance.new("UICorner")
FlingUICorner.CornerRadius = UDim.new(0, 8)
FlingUICorner.Parent = FlingBtn

local flingToggle = false
local lastPos = nil
local velConn = nil

local function resetCameraSubject()
    if workspace.CurrentCamera and lp.Character then
        local humanoid = lp.Character:FindFirstChildWhichIsA("Humanoid")
        if humanoid then
            workspace.CurrentCamera.CameraSubject = humanoid
        end
    end
end

local function createTween(hrp, targetCFrame)
    local tweenInfo = TweenInfo.new(TeleportTime, Enum.EasingStyle.Linear, Enum.EasingDirection.Out)
    return TweenService:Create(hrp, tweenInfo, {CFrame = targetCFrame})
end

local function startVelLoop(hrp)
    if velConn then velConn:Disconnect() end
    velConn = RunService.Heartbeat:Connect(function()
        if flingToggle and hrp and hrp.Parent then
            hrp.AssemblyLinearVelocity = Vector3.zero
            hrp.AssemblyAngularVelocity = Vector3.zero
        end
    end)
end

local function stopVelLoop()
    if velConn then
        velConn:Disconnect()
        velConn = nil
    end
end

local function toggleFling()
    local char = lp.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if flingToggle then
        flingToggle = false
        getgenv().IsFlinging = false 
        
        FlingBtn.Text = "🚀 Fling: OFF"
        FlingBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 255)
        stopVelLoop()
        
        if lastPos then
            local tweenBack = createTween(hrp, CFrame.new(lastPos))
            tweenBack:Play()
            tweenBack.Completed:Wait()
        end
        
        workspace.FallenPartsDestroyHeight = -500
        resetCameraSubject()
        if workspace:FindFirstChild("Gaze") then
            workspace.Gaze:Destroy()
        end
    else
        flingToggle = true
        getgenv().IsFlinging = true 
        
        lastPos = hrp.Position
        local part = Instance.new("Part")
        part.Size = Vector3.new(4, 1, 4)
        part.Position = lastPos
        part.Anchored = true
        part.CanCollide = false
        part.Transparency = 0.5
        part.Name = "Gaze"
        part.Parent = workspace
        
        if workspace.CurrentCamera then
            workspace.CurrentCamera.CameraSubject = part
        end
        
        FlingBtn.Text = "🚀 Fling: ON"
        FlingBtn.BackgroundColor3 = Color3.fromRGB(60, 220, 60)
        
        task.wait()
        workspace.FallenPartsDestroyHeight = -1000
        local tweenToTarget = createTween(hrp, CFrame.new(targetPos))
        tweenToTarget:Play()
        tweenToTarget.Completed:Wait()
        startVelLoop(hrp)
    end
end

FlingBtn.MouseButton1Click:Connect(toggleFling)

lp.CharacterAdded:Connect(function()
    if flingToggle then
        flingToggle = false
        getgenv().IsFlinging = false
        FlingBtn.Text = "🚀 Fling: OFF"
        FlingBtn.BackgroundColor3 = Color3.fromRGB(150, 50, 255)
        stopVelLoop()
        workspace.FallenPartsDestroyHeight = -500
        resetCameraSubject()
        if workspace:FindFirstChild("Gaze") then
            workspace.Gaze:Destroy()
        end
    end
end)

lp.CharacterAdded:Connect(function()
  IsInvisible = false; FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60); FloatBtn.Text = "Invis: ❌"
  IsHoldingMagnet = false; HoldMagBtn.BackgroundColor3 = Color3.fromRGB(60, 100, 220)
  removeLookGyro()
end)

local isInfJump = false; local isSpeedEnabled = false; local currentWalkSpeed = 35
UserInputService.JumpRequest:Connect(function()
  if isInfJump and lp.Character then
      local humanoid = lp.Character:FindFirstChildOfClass("Humanoid")
      if humanoid then humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end
  end
end)

-- ===============================================
-- [ 4 ] بناء واجهة Rayfield
-- ===============================================

if getgenv then
    getgenv().RayfieldKey = nil
    getgenv().RayfieldKeyValidated = nil
    getgenv().KeyValidated = nil
end

pcall(function()
    if isfile and delfile then
        local files = {
            "InvictusKeySave", "InvictusKeySave.txt", "InvictusKeySave.key",
            "Key.key", "Rayfield/InvictusKeySave.key", "Rayfield/Key.key"
        }
        for _, f in ipairs(files) do
            if isfile(f) then delfile(f) end
        end
    end
    if isfolder and delfolder and isfolder("Rayfield") then
        pcall(function() delfolder("Rayfield") end)
    end
end)

local Window = Rayfield:CreateWindow({
 Name = "Invictus v22",
 LoadingTitle = "Aura, Grab, ESP, avatar, animation & More",
 LoadingSubtitle = "by Invictus",
 Theme = "Amethyst",
 DisableRayfieldPrompts = false,
 DisableBuildWarnings = true,
 ConfigurationSaving = {
    Enabled = true,
    FolderName = "KHURAFI_Data",
    FileName = "AuraDeltaSettings"
 },
 KeySystem = true,
 KeySettings = {
    Title = "Key System",
    Subtitle = "أدخل المفتاح لتشغيل السكربت",
    Note = "new update anti-afk .new chat. bangs.And a lot of bug fixes",
    SaveKey = false,
    GrabKeyFromSite = true,
    Key = {"https://gist.githubusercontent.com/masterzaidxd2-web/e32fcc7d50306605945fc7e909c6fefc/raw/Password"}
 }
})

local MainTab = Window:CreateTab("Aura", 4483345998)
local GrabTab = Window:CreateTab("Grab", 4483345998)
local PlayerTab = Window:CreateTab("Player", 4483345998)
local WaypointsTab = Window:CreateTab("Waypoints", 4483345998)
local EspTab = Window:CreateTab("ESP", 4483345998)
local NameTab = Window:CreateTab("Name", 4483345998)
local SafeTab = Window:CreateTab("SAFE", 4483345998)
local EnemyTab = Window:CreateTab("Enemies", 4483345998)
local SettingsTab = Window:CreateTab("Settings", 4483345998)
local OthersTab = Window:CreateTab("Others", 4483345998)
local ChatTab = Window:CreateTab("🌐 Global Chat", 4483345998)

-- Aura Tab
-- Aura Tab
AuraToggleElement = MainTab:CreateToggle({ 
    Name = "Enable Aura (STATUS)", 
    CurrentValue = false, 
    Flag = "AuraToggle", 
    Callback = function(Value) 
        AuraEnabled = Value 
    end 
})
MainTab:CreateSlider({ Name = "Damage Range", Range = {5, 100}, Increment = 1, Suffix = "Studs", CurrentValue = 50, Flag = "RangeSlider", Callback = function(Value) rangeVal = Value end })
MainTab:CreateSlider({ Name = "Hit Speed", Range = {1, 60}, Increment = 1, Suffix = "Hit/s", CurrentValue = 15, Flag = "APSSlider", Callback = function(Value) apsVal = Value end })
local AuraTargetDropdown = MainTab:CreateDropdown({ Name = "Target", Options = getPlayerNames(true), CurrentOption = {"All"}, MultipleOptions = false, Flag = "AuraTargetDropdown", Callback = function(Options) if Options[1] then AuraTargetSelected = Options[1]; refreshAllEsp() end end })
MainTab:CreateButton({ Name = "Refresh Targets", Callback = function() AuraTargetDropdown:Refresh(getPlayerNames(true)) end })

-- Grab Tab
GrabTab:CreateSection("Anti-Grab & Revenge")
GrabTab:CreateToggle({ Name = "Auto-Escape & Target Attacker", CurrentValue = false, Flag = "AntiGrabToggle", Callback = function(Value) AntiGrabEnabled = Value end })
GrabTab:CreateToggle({ 
    Name = "Auto Aura Kill (If you are grabbed)", 
    CurrentValue = false, 
    Flag = "AutoAuraOnGrabToggle", 
    Callback = function(Value) 
        AutoAuraOnGrabEnabled = Value 
    end 
})

GrabTab:CreateSection("General Grab Settings")
GrabTab:CreateToggle({ Name = "Continuous Grab (All)", CurrentValue = false, Flag = "AutoGrabToggle", Callback = function(Value) AutoGrab = Value end })
GrabTab:CreateToggle({ Name = "Auto-Target (Anyone close)", CurrentValue = false, Flag = "AutoTargetToggle", Callback = function(Value) AutoTargetEnabled = Value end })

GrabTab:CreateSection("Specific Grab Settings (Smart)")
TargetDropdown1 = GrabTab:CreateDropdown({ Name = "Select Player (1)", Options = getPlayerNamesForTargets(), CurrentOption = {"None"}, MultipleOptions = false, Flag = "SpecificTargetDropdown1", Callback = function(Options) if Options[1] then ChosenTargetName1 = (Options[1] == "None") and "" or Options[1]; refreshAllEsp() end end })
TargetDropdown2 = GrabTab:CreateDropdown({ Name = "Select Player (2)", Options = getPlayerNamesForTargets(), CurrentOption = {"None"}, MultipleOptions = false, Flag = "SpecificTargetDropdown2", Callback = function(Options) if Options[1] then ChosenTargetName2 = (Options[1] == "None") and "" or Options[1]; refreshAllEsp() end end })
TargetDropdown3 = GrabTab:CreateDropdown({ Name = "Select Player (3)", Options = getPlayerNamesForTargets(), CurrentOption = {"None"}, MultipleOptions = false, Flag = "SpecificTargetDropdown3", Callback = function(Options) if Options[1] then ChosenTargetName3 = (Options[1] == "None") and "" or Options[1]; refreshAllEsp() end end })

GrabTab:CreateToggle({ Name = "Enable Specific Grab", CurrentValue = false, Flag = "SpecificTargetToggle", Callback = function(Value) SpecificTargetEnabled = Value; refreshAllEsp() end })
GrabTab:CreateSection("Best option Grab Repeat 5~8/ grab Activation 15 Studs maximum")
GrabTab:CreateSlider({ Name = "Grab Activation Range (Distance)", Range = {5, 100}, Increment = 1, Suffix = "Studs", CurrentValue = 15, Flag = "TriggerDistanceSlider", Callback = function(Value) TriggerDistance = Value end })
GrabTab:CreateSlider({ Name = "Grab Repeat Count", Range = {1, 15}, Increment = 1, Suffix = "Times", CurrentValue = 3, Flag = "GrabRepeatSlider", Callback = function(Value) grabRepeatVal = Value end })

GrabTab:CreateSection("Magnet & Aiming")
GrabTab:CreateToggle({ Name = "🎯 Auto Aim (Look at Target)", CurrentValue = false, Flag = "AutoLookToggle", Callback = function(Value) AutoLookEnabled = Value; if not Value then removeLookGyro() end end })
GrabTab:CreateSection("When this option is enabled, a 'Hold' button will appear on the screen. It activates once your grab points reach 100. Holding it down keeps you positioned behind the enemy to grab them")
GrabTab:CreateToggle({ Name = "Show Floating Magnet Button (For Combat)", CurrentValue = false, Flag = "HoldMagnetToggle", Callback = function(Value) MagnetGui.Enabled = Value; if not Value then IsHoldingMagnet = false end end })

GrabTab:CreateSection("Fast Teleport to Player")
local TpDropdown = GrabTab:CreateDropdown({ Name = "Teleport to Player", Options = getPlayerNames(false), CurrentOption = {""}, MultipleOptions = false, Flag = "TpDropdown", Callback = function(Options) TpSelectedPlayer = Options[1] end })
GrabTab:CreateButton({ Name = "Teleport", Callback = function() if TpSelectedPlayer ~= "" then local targetPlr = Players:FindFirstChild(TpSelectedPlayer) local myChar = lp.Character if targetPlr and targetPlr.Character and targetPlr.Character:FindFirstChild("HumanoidRootPart") and myChar and myChar:FindFirstChild("HumanoidRootPart") then myChar.HumanoidRootPart.CFrame = targetPlr.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 0.8) end end end })
GrabTab:CreateButton({ Name = "Teleport to GYM", Callback = function() if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then lp.Character.HumanoidRootPart.CFrame = CFrame.new(-121, 6, 528) end end })
GrabTab:CreateButton({ Name = "Refresh Player Lists", Callback = function() local n = getPlayerNamesForTargets() TargetDropdown1:Refresh(n); TargetDropdown2:Refresh(n); TargetDropdown3:Refresh(n); TpDropdown:Refresh(getPlayerNames(false)) end })

-- Player Tab & Fly System
local camera = workspace.CurrentCamera
local nowe = false
local flySpeed = 60
local flyConnection = nil

local function forceUnbindAndReset(chr)
    if not chr then return end
    local hum = chr:FindFirstChildWhichIsA("Humanoid")
    local root = chr:FindFirstChild("HumanoidRootPart") or chr:FindFirstChild("Torso")

    for _, obj in ipairs(chr:GetDescendants()) do
        if obj:IsA("Weld") or obj:IsA("WeldConstraint") or obj:IsA("ManualWeld") or obj:IsA("AlignPosition") or obj:IsA("AlignOrientation") then
            local p0, p1 = nil, nil
            if obj:IsA("AlignPosition") or obj:IsA("AlignOrientation") then
                p0 = obj.Attachment0 and obj.Attachment0.Parent
                p1 = obj.Attachment1 and obj.Attachment1.Parent
            else
                p0 = obj.Part0
                p1 = obj.Part1
            end
            if p0 and p1 then
                if not p0:IsDescendantOf(chr) or not p1:IsDescendantOf(chr) then
                    obj:Destroy()
                end
            end
        elseif obj.Name == "MobileFlyGyro" or obj.Name == "MobileFlyVelocity" then
            obj:Destroy()
        end
    end

    if root then
        root.AssemblyLinearVelocity = Vector3.zero
        root.AssemblyAngularVelocity = Vector3.zero
    end

    if hum then
        hum.PlatformStand = false
        hum.AutoRotate = true
        for _, state in ipairs(Enum.HumanoidStateType:GetEnumItems()) do
            pcall(function() hum:SetStateEnabled(state, true) end)
        end
        hum:ChangeState(Enum.HumanoidStateType.Physics)
        task.wait()
        hum:ChangeState(Enum.HumanoidStateType.GettingUp)
        task.wait()
        hum:ChangeState(Enum.HumanoidStateType.Running)

        if chr:FindFirstChild("Animate") then
            chr.Animate.Disabled = false
        end
    end
end

local function toggleFly(Value)
    nowe = Value
    local chr = lp.Character
    local hum = chr and chr:FindFirstChildWhichIsA("Humanoid")
    local root = chr and (chr:FindFirstChild("HumanoidRootPart") or chr:FindFirstChild("Torso"))
    
    if not chr or not hum or not root then return end

    if not nowe then
        if flyConnection then
            flyConnection:Disconnect()
            flyConnection = nil
        end
        forceUnbindAndReset(chr)
    else
        forceUnbindAndReset(chr) 
        hum.AutoRotate = false
        if chr:FindFirstChild("Animate") then
            chr.Animate.Disabled = true
        end
        for _, v in ipairs(hum:GetPlayingAnimationTracks()) do
            v:AdjustSpeed(0)
        end

        local bv = Instance.new("BodyVelocity")
        bv.Name = "MobileFlyVelocity"
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bv.Velocity = Vector3.zero
        bv.Parent = root
        
        local bg = Instance.new("BodyGyro")
        bg.Name = "MobileFlyGyro"
        bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        bg.P = 9000
        bg.Parent = root

        if flyConnection then flyConnection:Disconnect() end
        
        flyConnection = RunService.RenderStepped:Connect(function()
            local currentChr = lp.Character
            local currentHum = currentChr and currentChr:FindFirstChildWhichIsA("Humanoid")
            local currentRoot = currentChr and (currentChr:FindFirstChild("HumanoidRootPart") or currentChr:FindFirstChild("Torso"))
            
            if currentChr and currentHum and currentRoot and nowe then
                currentHum:ChangeState(Enum.HumanoidStateType.Swimming)
                local camCF = camera.CFrame
                bg.CFrame = camCF
                local moveDir = currentHum.MoveDirection
                if moveDir.Magnitude > 0 then
                    local localMove = camCF:VectorToObjectSpace(moveDir)
                    local flightDir = camCF:VectorToWorldSpace(localMove)
                    bv.Velocity = flightDir * flySpeed
                else
                    bv.Velocity = Vector3.zero
                end
            else
                if flyConnection then
                    flyConnection:Disconnect()
                    flyConnection = nil
                end
            end
        end)
    end
end

lp.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    forceUnbindAndReset(char)
    if nowe then
        task.wait(0.1)
        toggleFly(true)
    end
end)

lp.CharacterAdded:Connect(function(char)
    task.wait(1)
    local hum = char:WaitForChild("Humanoid", 3)
    if hum then
        hum.PlatformStand = false
        pcall(function()
            hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, not AntiFallEnabled)
            hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, not AntiFallEnabled)
        end)
    end
    local root = char:WaitForChild("HumanoidRootPart", 3)
    if root then
        root.AssemblyLinearVelocity = Vector3.zero
        root.AssemblyAngularVelocity = Vector3.zero
    end
    if char:FindFirstChild("Animate") then
        char.Animate.Disabled = false
    end
end)

local NoclipConnection = nil
local function noclip()
    if NoclipConnection then NoclipConnection:Disconnect() end
    NoclipConnection = RunService.Stepped:Connect(function()
        if lp.Character then
            for _, v in ipairs(lp.Character:GetDescendants()) do
                if v:IsA("BasePart") then
                    v.CanCollide = false
                end
            end
        end
    end)
end

local function clip()
    if NoclipConnection then
        NoclipConnection:Disconnect()
        NoclipConnection = nil
    end
    if lp.Character then
        for _, v in ipairs(lp.Character:GetDescendants()) do
            if v:IsA("BasePart") then
                if v.Name == "Head" or v.Name == "Torso" or v.Name == "UpperTorso" or v.Name == "LowerTorso" then
                    v.CanCollide = true
                else
                    v.CanCollide = false
                end
            end
        end
    end
end

local PlayerCollisionEnabled = false
local PlayerCollisionConn = nil
local LocalStands = {}

local function cleanupStands()
    for _, stand in pairs(LocalStands) do
        if stand and stand.Parent then stand:Destroy() end
    end
    LocalStands = {}
end

local function enablePlayerCollisions()
    if PlayerCollisionConn then PlayerCollisionConn:Disconnect() end
    PlayerCollisionConn = RunService.Stepped:Connect(function()
        if not PlayerCollisionEnabled then return end
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= lp and player.Character then
                local head = player.Character:FindFirstChild("Head")
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                if head and hum and hum.Health > 0 then
                    local stand = LocalStands[player.Name]
                    if not stand or not stand.Parent then
                        stand = Instance.new("Part")
                        stand.Name = "LocalHeadStand"
                        stand.Size = Vector3.new(2.5, 0.4, 2.5)
                        stand.Transparency = 1
                        stand.Anchored = true
                        stand.CanCollide = true
                        stand.Parent = workspace
                        LocalStands[player.Name] = stand
                    end
                    stand.CFrame = head.CFrame * CFrame.new(0, 0.7, 0)
                else
                    if LocalStands[player.Name] then
                        LocalStands[player.Name]:Destroy()
                        LocalStands[player.Name] = nil
                    end
                end
            end
        end
    end)
end

local function disablePlayerCollisions()
    if PlayerCollisionConn then
        PlayerCollisionConn:Disconnect()
        PlayerCollisionConn = nil
    end
    cleanupStands()
end

PlayerTab:CreateSection("Custom Fly System")
PlayerTab:CreateToggle({ Name = "🕊️ Enable Fly", CurrentValue = false, Flag = "FlyToggle", Callback = function(Value) toggleFly(Value) end })
PlayerTab:CreateSlider({ Name = "Fly Speed", Range = {10, 150}, Increment = 1, Suffix = "Speed", CurrentValue = 60, Flag = "FlySpeedSlider", Callback = function(Value) flySpeed = Value end })
PlayerTab:CreateSection("SPEED WALK")
PlayerTab:CreateToggle({ Name = "Lock WalkSpeed", CurrentValue = false, Flag = "SpeedToggle", Callback = function(Value) isSpeedEnabled = Value; if not Value and lp.Character then local hum = lp.Character:FindFirstChildOfClass("Humanoid") if hum then hum.WalkSpeed = 16 end end end })
PlayerTab:CreateSlider({ Name = "WalkSpeed Value", Range = {16, 150}, Increment = 1, Suffix = "WS", CurrentValue = 35, Flag = "SpeedSlider", Callback = function(Value) currentWalkSpeed = Value end })
PlayerTab:CreateSection("Ghost Mode button 👻")
PlayerTab:CreateToggle({ Name = "Show Floating Invis Button", CurrentValue = false, Flag = "InvisSystemToggle", Callback = function(Value) InvisGui.Enabled = Value; if not Value and IsInvisible then IsInvisible = false; FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60); FloatBtn.Text = "Invis: ❌"; for _, item in pairs(invisParts) do if item.obj and item.obj.Parent then item.obj.Transparency = item.origTrans end end end end })
PlayerTab:CreateSection("Character Abilities")
PlayerTab:CreateToggle({ Name = "Infinite Jump", CurrentValue = false, Flag = "InfJumpToggle", Callback = function(Value) isInfJump = Value end })
PlayerTab:CreateToggle({ Name = "Show Floating Fling Button", CurrentValue = false, Flag = "FlingBtnToggle", Callback = function(Value) FlingGui.Enabled = Value end })
PlayerTab:CreateToggle({ Name = "🧱 Noclip", CurrentValue = false, Flag = "NoclipToggle", Callback = function(Value) if Value then noclip() else clip() end end })
PlayerTab:CreateToggle({ Name = "👥 Stand on Players' Heads", CurrentValue = false, Flag = "PlayerCollisionToggle", Callback = function(Value) PlayerCollisionEnabled = Value; if Value then enablePlayerCollisions() else disablePlayerCollisions() end end })

PlayerTab:CreateSection("Security & Anti")
local AntiRagdollConnection
local CharAddedConnection

-- دالة ذكية للتحقق مما إذا كان اللاعب يتعرض للإمساك حالياً
local function isBeingGrabbed(character)
    for _, descendant in ipairs(character:GetDescendants()) do
        if descendant:IsA("Constraint") or descendant:IsA("Weld") or descendant:IsA("WeldConstraint") then
            local name = descendant.Name:lower()
            if name:find("grab") or name:find("carry") or name:find("hold") or name:find("stun") then
                return true
            end
        end
    end
    return false
end

-- دالة تشغيل Anti-Ragdoll
local function startAntiRagdoll(character)
    if not character then return end
    local hu = character:WaitForChild("Humanoid", 3)
    if not hu then return end

    if AntiRagdollConnection then AntiRagdollConnection:Disconnect() end

    AntiRagdollConnection = game:GetService("RunService").Heartbeat:Connect(function()
        if not character or not character.Parent then return end

        -- إذا كنت متلبساً بحالة إمساك، يتوقف السكربت مؤقتاً لكي يعمل Anti-Grab
        if isBeingGrabbed(character) then
            return
        end

        -- منع الاستلقاء والجلوس
        if hu.PlatformStand then hu.PlatformStand = false end
        if hu.Sit then hu.Sit = false end

        -- النهوض الفوري
        local state = hu:GetState()
        if state == Enum.HumanoidStateType.Ragdoll 
           or state == Enum.HumanoidStateType.FallingDown 
           or state == Enum.HumanoidStateType.PlatformStanding 
           or state == Enum.HumanoidStateType.Physics then
            hu:ChangeState(Enum.HumanoidStateType.GettingUp)
        end

        -- إرجاع المفاصل وتدمير قيود الـ Ragdoll
        for _, o in ipairs(character:GetDescendants()) do
            if o:IsA("Motor6D") and not o.Enabled then
                o.Enabled = true
            elseif (o:IsA("BaseConstraint") or o:IsA("Attachment")) and o.Name:lower():find("ragdoll") then
                o:Destroy()
            end
        end
    end)
end

PlayerTab:CreateToggle({
    Name = "🚫 Anti-Ragdoll (Advanced)",
    CurrentValue = false,
    Flag = "AntiRagdollToggle",
    Callback = function(Value)
        local player = game:GetService("Players").LocalPlayer
        AntiFallEnabled = Value

        if Value then
            startAntiRagdoll(player.Character)

            if CharAddedConnection then CharAddedConnection:Disconnect() end
            CharAddedConnection = player.CharacterAdded:Connect(function(newChar)
                startAntiRagdoll(newChar)
            end)
        else
            if AntiRagdollConnection then
                AntiRagdollConnection:Disconnect()
                AntiRagdollConnection = nil
            end
            if CharAddedConnection then
                CharAddedConnection:Disconnect()
                CharAddedConnection = nil
            end
        end
    end,
})

local VoidAntiFallConn = nil
local lastSafeCFrame = CFrame.new(0, 10, 0)

PlayerTab:CreateToggle({ 
    Name = "🕳️ Void Anti-Fall (Save from Void)", 
    CurrentValue = false, 
    Flag = "VoidAntiFallToggle", 
    Callback = function(Value) 
        if Value then
            if VoidAntiFallConn then VoidAntiFallConn:Disconnect() end
            VoidAntiFallConn = RunService.Heartbeat:Connect(function()
                local char = lp.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                local hum = char and char:FindFirstChild("Humanoid")
                if root and hum and hum.Health > 0 then
                    if getgenv().IsFlinging then return end 
                    if root.Position.Y > -50 then
                        if hum.FloorMaterial ~= Enum.Material.Air then 
                            lastSafeCFrame = root.CFrame 
                        end
                    else
                        root.AssemblyLinearVelocity = Vector3.zero
                        root.CFrame = lastSafeCFrame + Vector3.new(0, 3, 0)
                    end
                end
            end)
        else
            if VoidAntiFallConn then
                VoidAntiFallConn:Disconnect()
                VoidAntiFallConn = nil
            end
        end
    end 
})
-- Walk Fling System
 -- Walk Fling System
local walkFlingConnection = nil
local antiWalkFlingToggleState = false
local walkFlingPower = 50000 
local isHugging = false
local isGrabPause = false

local function notify(title, text)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {Title = title, Text = text, Duration = 3})
    end)
end

local function stopWalkFling()
    if walkFlingConnection then
        walkFlingConnection:Disconnect()
        walkFlingConnection = nil
    end
    if lp.Character then
        for _, part in ipairs(lp.Character:GetChildren()) do
            if part:IsA("BasePart") then
                pcall(function()
                    part.AssemblyLinearVelocity = Vector3.zero
                    part.AssemblyAngularVelocity = Vector3.zero
                end)
            end
        end
    end
end

local function startWalkFling()
    stopWalkFling()
    if not antiWalkFlingToggleState or isHugging or isGrabPause then return end

    local char = lp.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    local root = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso")
    if not hum or hum.Health <= 0 or not root then return end

    local movel = 0.1
    walkFlingConnection = RunService.Heartbeat:Connect(function()
        if isHugging or isGrabPause or not antiWalkFlingToggleState then return end
        local currentChar = lp.Character
        local currentRoot = currentChar and (currentChar:FindFirstChild("HumanoidRootPart") or currentChar:FindFirstChild("Torso"))
        local currentHum = currentChar and currentChar:FindFirstChildOfClass("Humanoid")
        if not currentRoot or not currentHum then return end

        if currentHum.MoveDirection.Magnitude > 0 then
            return
        end

        pcall(function()
            local oldVel = currentRoot.AssemblyLinearVelocity
            currentRoot.AssemblyLinearVelocity = Vector3.new(walkFlingPower, walkFlingPower, walkFlingPower)
            RunService.RenderStepped:Wait()
            if isHugging or isGrabPause or not antiWalkFlingToggleState or currentHum.MoveDirection.Magnitude > 0 then
                currentRoot.AssemblyLinearVelocity = Vector3.zero
                return
            end
            currentRoot.AssemblyLinearVelocity = oldVel
            RunService.Stepped:Wait()
            if isHugging or isGrabPause or not antiWalkFlingToggleState or currentHum.MoveDirection.Magnitude > 0 then
                currentRoot.AssemblyLinearVelocity = Vector3.zero
                return
            end
            currentRoot.AssemblyLinearVelocity = oldVel + Vector3.new(0, movel, 0)
            movel = movel * -1
        end)
    end)
end

pcall(function()
    if not hookmetamethod then return end
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        local args = {...}
        local method = getnamecallmethod and getnamecallmethod() or ""
        if not checkcaller() and method == "InvokeServer" then
            if self.Name == "StartJointEmote" then
                local anim = args[1]
                if typeof(anim) == "Instance" and (anim.Name:find("Hold") or anim.Name:find("Hug") or (anim.Parent and anim.Parent.Name == "Hugs")) then
                    if not isHugging then
                        isHugging = true
                        task.spawn(function()
                            stopWalkFling()
                            notify("حماية الفلينق", "تم إيقاف الفلينق 🫂 (احتضان آمن)")
                        end)
                    end
                end
            elseif self.Name == "CancelJointEmote" then
                if isHugging then
                    task.spawn(function()
                        notify("حماية الفلينق", "تم الإفلات.. ننتظر ابتعاد الشخصية ⏳")
                        task.wait(2) 
                        isHugging = false
                        if antiWalkFlingToggleState and not isGrabPause then
                            startWalkFling()
                            notify("حماية الفلينق", "تم إعادة تشغيل الفلينق ⚠️")
                        end
                    end)
                end
            end
        end
        return oldNamecall(self, unpack(args))
    end)
end)

local function setupCharacter(char)
    stopWalkFling()
    isHugging = false
    isGrabPause = false

    local hum = char:WaitForChild("Humanoid", 5)
    if hum then 
        hum.Died:Connect(function() stopWalkFling() end) 
        
        local function handleGrabPause()
            if isGrabPause or not antiWalkFlingToggleState then return end
            isGrabPause = true
            stopWalkFling()
            notify("حماية الفلينق", "تم إيقاف الفلينق لـ 6 ثوانٍ لأنك تقوم بإمساك لاعب 🛑")
            
            task.spawn(function()
                task.wait(6)
                isGrabPause = false
                if antiWalkFlingToggleState and not isHugging then
                    startWalkFling()
                    notify("حماية الفلينق", "عاد الفلينق للعمل ⚠️")
                end
            end)
        end

        -- كشف عندما تقوم أنت بإمساك لاعب آخر (ربط جزء من شخصيتك بجزء من شخصيته أثناء وقوفك الطبيعي)
        char.DescendantAdded:Connect(function(child)
            task.wait(0.05)
            if child:IsA("Weld") or child:IsA("WeldConstraint") or child:IsA("Motor6D") then
                local p0, p1 = child.Part0, child.Part1
                if p0 and p1 then
                    local char0 = p0:FindFirstAncestorOfClass("Model")
                    local char1 = p1:FindFirstAncestorOfClass("Model")
                    
                    -- التأكد أنك أنت الساحب وليس المسحوب (حالتك ليست PlatformStanding أو Physics)
                    if (char0 == char and char1 ~= char and char1:FindFirstChildOfClass("Humanoid")) or
                       (char1 == char and char0 ~= char and char0:FindFirstChildOfClass("Humanoid")) then
                        local state = hum:GetState()
                        if state ~= Enum.HumanoidStateType.PlatformStanding and state ~= Enum.HumanoidStateType.Physics then
                            handleGrabPause()
                        end
                    end
                end
            end
        end)
    end
    
    if antiWalkFlingToggleState then
        task.wait(1)
        startWalkFling()
    end
end

lp.CharacterAdded:Connect(setupCharacter)
if lp.Character then setupCharacter(lp.Character) end

PlayerTab:CreateToggle({
    Name = "Auto-Safe Walk Fling (Perfect Edition)",
    CurrentValue = false,
    Flag = "AntiWalkFlingToggle", 
    Callback = function(Value)
        antiWalkFlingToggleState = Value
        if Value then startWalkFling() else stopWalkFling() end
    end,
})
-- ===============================================
-- [ Anti-AFK System ]
-- ===============================================
local VirtualUser = game:GetService("VirtualUser")
local antiAfkConnection = nil

PlayerTab:CreateToggle({
    Name = "Anti-AFK (منع الطرد)",
    CurrentValue = false,
    Flag = "AntiAFK", 
    Callback = function(Value)
        if Value then
            -- تفعيل النظام
            if not antiAfkConnection then
                antiAfkConnection = lp.Idled:Connect(function()
                    VirtualUser:CaptureController()
                    VirtualUser:ClickButton2(Vector2.zero)
                end)
            end
            Rayfield:Notify({
                Title = "Anti-AFK",
                Content = "تم تفعيل منع الطرد من السيرفر بنجاح.",
                Duration = 2
            })
        else
            -- إيقاف النظام
            if antiAfkConnection then
                antiAfkConnection:Disconnect()
                antiAfkConnection = nil
            end
            Rayfield:Notify({
                Title = "Anti-AFK",
                Content = "تم إيقاف منع الطرد.",
                Duration = 2
            })
        end
    end,
})
 -- ===============================================
-- إعدادات الـ Spinbot (دوران الجسد بالكامل)
-- ===============================================
local SpinbotEnabled = false
local SpinbotSpeed = 25

-- تأكد من أن 'PlayerTab' هو اسم المتغير الصحيح لخانة اللاعب في السكربت الخاص بك
PlayerTab:CreateSection("🌀 Spinbot (Full Body 360° Close it if you want a specific target)")

PlayerTab:CreateToggle({ 
    Name = "Enable Spinbot", 
    CurrentValue = false, 
    Flag = "SpinbotToggle", 
    Callback = function(Value) 
        SpinbotEnabled = Value 
        -- تنظيف الـ Gyro عند الإيقاف ليعود الجسم لحالته الطبيعية
        if not Value and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
            local bg = lp.Character.HumanoidRootPart:FindFirstChild("SpinbotGyro")
            if bg then bg:Destroy() end
        end
    end 
})

PlayerTab:CreateSlider({ 
    Name = "Spin Speed", 
    Range = {5, 100}, 
    Increment = 1, 
    Suffix = "Speed", 
    CurrentValue = 25, 
    Flag = "SpinbotSpeedSlider", 
    Callback = function(Value) 
        SpinbotSpeed = Value 
    end 
})

-- Waypoints Tab
WaypointsTab:CreateSection("Saved Waypoints Management")
local NewWaypointName = ""
WaypointsTab:CreateInput({ Name = "Waypoint Name", PlaceholderText = "Enter waypoint name...", RemoveTextAfterFocusLost = false, Callback = function(Text) NewWaypointName = Text end })
local WaypointDropdown
WaypointsTab:CreateButton({ Name = "📌 Save Current Location", Callback = function() if NewWaypointName ~= "" and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then SavedWaypoints[NewWaypointName] = lp.Character.HumanoidRootPart.CFrame; saveData(); Rayfield:Notify({Title = "Saved", Content = NewWaypointName, Duration = 2}); if WaypointDropdown then WaypointDropdown:Refresh(getWaypointNames()) end else Rayfield:Notify({Title = "Error", Content = "Enter a name", Duration = 2}) end end })
WaypointDropdown = WaypointsTab:CreateDropdown({ Name = "Waypoints List", Options = getWaypointNames(), CurrentOption = {""}, MultipleOptions = false, Flag = "WaypointsDropdownList", Callback = function(Options) if Options[1] and Options[1] ~= "No saved waypoints" then SelectedWaypointName = Options[1] end end })
WaypointsTab:CreateButton({ Name = "🚀 Teleport to Selected Waypoint", Callback = function() if SelectedWaypointName and SavedWaypoints[SelectedWaypointName] and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then lp.Character.HumanoidRootPart.CFrame = SavedWaypoints[SelectedWaypointName] end end })
WaypointsTab:CreateButton({ Name = "🗑️ Delete Selected Waypoint", Callback = function() if SelectedWaypointName and SavedWaypoints[SelectedWaypointName] then SavedWaypoints[SelectedWaypointName] = nil; saveData(); SelectedWaypointName = nil; if WaypointDropdown then WaypointDropdown:Refresh(getWaypointNames()) end end end })

-- ESP Tab
EspTab:CreateToggle({ Name = "Enable ESP", CurrentValue = false, Flag = "EspMainToggle", Callback = function(Value) EspEnabled = Value; refreshAllEsp() end })
EspTab:CreateToggle({ Name = "Highlight (Boxes)", CurrentValue = true, Flag = "EspBoxesToggle", Callback = function(Value) EspBoxes = Value; refreshAllEsp() end })
EspTab:CreateToggle({ Name = "Names & Distance", CurrentValue = true, Flag = "EspNamesToggle", Callback = function(Value) EspNames = Value; refreshAllEsp() end })

-- Name & Bio & Outfit Copier Tab
local BWNameActive = false
local currentNameMode = "NONE"
local rnHue = 0
local bwLastUpdate = 0
local rnSpeed = 0.7
local rnUpdateRate = 0.02
local policeToggle = false  

local BioEffectActive = false
local currentBioMode = "NONE"
local bioHue = 0
local bioLastUpdate = 0
local bioSpeed = 0.7
local bioUpdateRate = 0.02
local bioPoliceToggle = false  

local bioColorRemote = nil
local remotes = ReplicatedStorage:FindFirstChild("Remotes")
if remotes then
    bioColorRemote = remotes:FindFirstChild("SetBioColor") or remotes:FindFirstChild("UpdateBioColor")
end

local rainbowNameRemote = ReplicatedStorage:WaitForChild("Remotes", 10)
rainbowNameRemote = rainbowNameRemote and rainbowNameRemote:FindFirstChild("SetRPNameColor")

NameTab:CreateToggle({ Name = "Enable Effects", CurrentValue = false, Flag = "NameEffectToggle", Callback = function(Value) BWNameActive = Value end })
NameTab:CreateDropdown({ Name = "Type", Options = {"BW", "Rainbow", "Pulse", "Strobe", "Police"}, CurrentOption = {"Rainbow"}, MultipleOptions = false, Flag = "NameEffectModeDropdown", Callback = function(Options) currentNameMode = Options[1] end })
NameTab:CreateSlider({ Name = "Speed", Range = {0.1, 3.0}, Increment = 0.1, Suffix = "x", CurrentValue = 0.7, Flag = "NameSpeedSlider", Callback = function(Value) rnSpeed = Value end })

NameTab:CreateSection("🧬 Bio Effects")
NameTab:CreateToggle({ Name = "Enable Bio Effects", CurrentValue = false, Flag = "BioEffectToggle", Callback = function(Value) BioEffectActive = Value end })
NameTab:CreateDropdown({ Name = "Bio Effect Type", Options = {"BW", "Rainbow", "Pulse", "Strobe", "Police"}, CurrentOption = {"Rainbow"}, MultipleOptions = false, Flag = "BioEffectModeDropdown", Callback = function(Options) currentBioMode = Options[1] end })
NameTab:CreateSlider({ Name = "Bio Speed", Range = {0.1, 3.0}, Increment = 0.1, Suffix = "x", CurrentValue = 0.7, Flag = "BioSpeedSlider", Callback = function(Value) bioSpeed = Value end })

RunService.Heartbeat:Connect(function(dt)
    if rainbowNameRemote and BWNameActive then
        local now = os.clock()
        rnHue = (rnHue + (dt * rnSpeed)) % 1
        if now - bwLastUpdate >= (currentNameMode == "Police" and 0.12 or rnUpdateRate) then
            local targetColor
            if currentNameMode == "BW" then 
                local intensity = (math.sin(rnHue * math.pi * 2) + 1) / 2
                targetColor = Color3.new(intensity, intensity, intensity)
            elseif currentNameMode == "Rainbow" then 
                targetColor = Color3.fromHSV(rnHue, 1, 1)
            elseif currentNameMode == "Pulse" then 
                targetColor = Color3.fromHSV(rnHue, 0.8, 1)
            elseif currentNameMode == "Strobe" then 
                targetColor = Color3.fromRGB(math.random(50,255), math.random(50,255), math.random(50,255))
            elseif currentNameMode == "Police" then 
                policeToggle = not policeToggle
                targetColor = policeToggle and Color3.fromRGB(0, 100, 255) or Color3.fromRGB(255, 0, 0) 
            end
            if targetColor then 
                pcall(function() rainbowNameRemote:FireServer(targetColor) end) 
            end
            bwLastUpdate = now
        end
    end
end)

RunService.Heartbeat:Connect(function(dt)
    if bioColorRemote and BioEffectActive then
        local now = os.clock()
        bioHue = (bioHue + (dt * bioSpeed)) % 1
        if now - bioLastUpdate >= (currentBioMode == "Police" and 0.12 or bioUpdateRate) then
            local targetColor
            if currentBioMode == "BW" then 
                local intensity = (math.sin(bioHue * math.pi * 2) + 1) / 2
                targetColor = Color3.new(intensity, intensity, intensity)
            elseif currentBioMode == "Rainbow" then 
                targetColor = Color3.fromHSV(bioHue, 1, 1)
            elseif currentBioMode == "Pulse" then 
                targetColor = Color3.fromHSV(bioHue, 0.8, 1)
            elseif currentBioMode == "Strobe" then 
                targetColor = Color3.fromRGB(math.random(50,255), math.random(50,255), math.random(50,255))
            elseif currentBioMode == "Police" then 
                bioPoliceToggle = not bioPoliceToggle
                targetColor = bioPoliceToggle and Color3.fromRGB(0, 100, 255) or Color3.fromRGB(255, 0, 0) 
            end
            if targetColor then 
                pcall(function() bioColorRemote:FireServer(targetColor) end) 
            end
            bioLastUpdate = now
        end
    end
end)

NameTab:CreateSection("🎭 Outfit Copier System")
local selectedOutfitTarget = ""

local function getOutfitPlayerNames()
    local names = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= lp then table.insert(names, p.Name) end
    end
    if #names == 0 then table.insert(names, "No players") end
    return names
end

local OutfitPlayerDropdown = NameTab:CreateDropdown({
    Name = "Select Player to Copy Outfit",
    Options = getOutfitPlayerNames(),
    CurrentOption = {""},
    MultipleOptions = false,
    Flag = "OutfitPlayerDropdown",
    Callback = function(Options)
        if Options[1] and Options[1] ~= "No players" then
            selectedOutfitTarget = Options[1]
        else
            selectedOutfitTarget = ""
        end
    end
})

NameTab:CreateButton({ Name = "🔄 Refresh Players List", Callback = function() OutfitPlayerDropdown:Refresh(getOutfitPlayerNames()) end })

local function copyOutfitDirectly(targetPlayer)
    local BloxbizRemotes = ReplicatedStorage:FindFirstChild("BloxbizRemotes")
    if not BloxbizRemotes then return false, "لم يتم العثور على BloxbizRemotes" end
    local targetHum = targetPlayer.Character and targetPlayer.Character:FindFirstChildOfClass("Humanoid")
    if not targetHum then return false, "اللاعب غير مترسبن حالياً" end
    local desc = targetHum:GetAppliedDescription()
    if not desc then return false, "فشل جلب بيانات السكن" end

    if BloxbizRemotes:FindFirstChild("CatalogOnResetOutfit") then
        BloxbizRemotes.CatalogOnResetOutfit:FireServer()
        task.wait(0.15)
    end

    if BloxbizRemotes:FindFirstChild("CatalogOnApplyOutfit") then
        local outfitArgs = {
            [1] = {
                ["RightLegColor"] = desc.RightLegColor,
                ["Torso"] = desc.Torso,
                ["ProportionScale"] = desc.ProportionScale,
                ["BodyTypeScale"] = desc.BodyTypeScale,
                ["LeftArmColor"] = desc.LeftArmColor,
                ["Pants"] = desc.Pants,
                ["RightArmColor"] = desc.RightArmColor,
                ["RightArm"] = desc.RightArm,
                ["DepthScale"] = desc.DepthScale,
                ["Head"] = desc.Head,
                ["WidthScale"] = desc.WidthScale,
                ["Shirt"] = desc.Shirt,
                ["LeftLegColor"] = desc.LeftLegColor,
                ["GraphicTShirt"] = desc.GraphicTShirt,
                ["HeadColor"] = desc.HeadColor,
                ["TorsoColor"] = desc.TorsoColor,
                ["RightLeg"] = desc.RightLeg,
                ["HeadScale"] = desc.HeadScale,
                ["HeightScale"] = desc.HeightScale,
                ["LeftArm"] = desc.LeftArm,
                ["LeftLeg"] = desc.LeftLeg,
                ["Accessories"] = desc:GetAccessories(true)
            }
        }
        BloxbizRemotes.CatalogOnApplyOutfit:FireServer(unpack(outfitArgs))
        return true, "تم نسخ سكن @" .. targetPlayer.Name
    end
    return false, "فشل إرسال الأمر للسيرفر"
end

NameTab:CreateButton({
    Name = "📥 تطبيق السكن (Apply Outfit)",
    Callback = function()
        if selectedOutfitTarget == "" then
            Rayfield:Notify({Title = "Outfit Copier", Content = "الرجاء اختيار لاعب أولاً!", Duration = 3})
            return
        end
        local targetPlr = Players:FindFirstChild(selectedOutfitTarget)
        if not targetPlr then
            Rayfield:Notify({Title = "Outfit Copier", Content = "لم يتم العثور على اللاعب في السيرفر!", Duration = 3})
            return
        end
        local success, msg = copyOutfitDirectly(targetPlr)
        Rayfield:Notify({Title = "Outfit Copier", Content = (success and "" or "❌ ") .. msg, Duration = 3})
    end
})

NameTab:CreateButton({
    Name = "♻️ ترصيت السكن الحالي (Reset Outfit)",
    Callback = function()
        local BloxbizRemotes = ReplicatedStorage:FindFirstChild("BloxbizRemotes")
        if BloxbizRemotes and BloxbizRemotes:FindFirstChild("CatalogOnResetOutfit") then
            BloxbizRemotes.CatalogOnResetOutfit:FireServer()
            Rayfield:Notify({Title = "Outfit Copier", Content = "تم إعادة سكنك الأصلي بنجاح", Duration = 3})
        else
            Rayfield:Notify({Title = "Outfit Copier", Content = "❌ لم يتم العثور على ريموت إعادة السكن", Duration = 3})
        end
    end
})

-- ===============================================
-- SAFE Tab
-- ===============================================
SafeTab:CreateSection("Safe Players List (10 Slots)")
local SafeDropdowns = {}

for i = 1, 10 do
    SafeDropdowns[i] = SafeTab:CreateDropdown({
        Name = "SAFE " .. i,
        Options = getPlayerNamesWithNone(),
        CurrentOption = {SAVE["safe"..i] ~= "" and SAVE["safe"..i] or "None"},
        MultipleOptions = false,
        Flag = "SafeDropdown" .. i,
        Callback = function(Options)
            if Options[1] then
                SAVE["safe"..i] = (Options[1] == "None") and "" or Options[1]
                saveData()
                refreshAllEsp()
            end
        end
    })
end

SafeTab:CreateButton({
  Name = "Refresh Safe Players List",
  Callback = function()
      local names = getPlayerNamesWithNone()
      for i = 1, 10 do
          SafeDropdowns[i]:Refresh(names)
      end
  end
})

-- ===============================================
-- Enemies Tab (قائمة الاستهداف اليدوي للأعداء)
-- ===============================================
EnemyTab:CreateSection("Manual Enemy Target List (10 Slots)")
local EnemyDropdowns = {}

for i = 1, 10 do
    EnemyDropdowns[i] = EnemyTab:CreateDropdown({
        Name = "ENEMY TARGET " .. i,
        Options = getPlayerNamesWithNone(),
        CurrentOption = {SAVE["enemy"..i] ~= "" and SAVE["enemy"..i] or "None"},
        MultipleOptions = false,
        Flag = "EnemyDropdown" .. i,
        Callback = function(Options)
            if Options[1] then
                SAVE["enemy"..i] = (Options[1] == "None") and "" or Options[1]
                saveData()
                refreshAllEsp()
            end
        end
    })
end

EnemyTab:CreateButton({
  Name = "Refresh Enemies List",
  Callback = function()
      local names = getPlayerNamesWithNone()
      for i = 1, 10 do
          EnemyDropdowns[i]:Refresh(names)
      end
  end
})


-- Others Tab
local EmoteLoaded = false

OthersTab:CreateSection("🎭 Emotes & Animations")
OthersTab:CreateToggle({
    Name = "Emote Script",
    CurrentValue = false,
    Flag = "EmoteToggle",
    Callback = function(Value)
        local function toggleGuiVisibility(state)
            local coreGui = game:GetService("CoreGui")
            local playerGui = game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui")
            local function searchAndToggle(parent)
                if not parent then return end
                for _, gui in pairs(parent:GetChildren()) do
                    local name = gui.Name:lower()
                    if (name:find("emote") or name:find("emte")) and not name:find("rayfield") then
                        if gui:IsA("ScreenGui") then
                            gui.Enabled = state
                        end
                    end
                end
            end
            searchAndToggle(coreGui)
            searchAndToggle(playerGui)
        end

        if Value then
            if not EmoteLoaded then
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/masterzaidxd2-web/Invictus-/refs/heads/main/emte"))()
                end)
                EmoteLoaded = true
            else
                pcall(function() toggleGuiVisibility(true) end)
            end
        else
            pcall(function() toggleGuiVisibility(false) end)
        end
    end
})

OthersTab:CreateSection("🌍 Server & Teleport Utility")
OthersTab:CreateButton({
    Name = "🔄 Rejoin Current Server",
    Callback = function()
        local TeleportService = game:GetService("TeleportService")
        Rayfield:Notify({Title = "Server", Content = "Rejoining server...", Duration = 3})
        if #Players:GetPlayers() <= 1 then
            lp:Kick("\nRejoining...")
            task.wait()
            TeleportService:Teleport(game.PlaceId, lp)
        else
            TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, lp)
        end
    end
})

OthersTab:CreateButton({
    Name = "⏭️ Server Hop (Join Different Server)",
    Callback = function()
        local HttpService = game:GetService("HttpService")
        local TeleportService = game:GetService("TeleportService")
        local req = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        
        if not req then
            Rayfield:Notify({Title = "Error", Content = "المشغل الخاص بك لا يدعم طلبات HTTP.", Duration = 3})
            return
        end

        Rayfield:Notify({Title = "Server Hop", Content = "جاري البحث عن سيرفر جديد...", Duration = 3})

        task.spawn(function()
            pcall(function()
                local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&excludeFullGames=true&limit=100"
                local response = req({Url = url, Method = "GET"})
                if response and response.StatusCode == 200 then
                    local data = HttpService:JSONDecode(response.Body)
                    if data and data.data then
                        local availableServers = {}
                        for _, server in ipairs(data.data) do
                            if type(server) == "table" and server.id ~= game.JobId and server.playing < server.maxPlayers then
                                table.insert(availableServers, server.id)
                            end
                        end
                        if #availableServers > 0 then
                            local targetServer = availableServers[math.random(1, #availableServers)]
                            Rayfield:Notify({Title = "Teleporting", Content = "تم إيجاد سيرفر! جاري الانتقال...", Duration = 3})
                            TeleportService:TeleportToPlaceInstance(game.PlaceId, targetServer, game.Players.LocalPlayer)
                        else
                            Rayfield:Notify({Title = "Server Hop", Content = "لا توجد سيرفرات أخرى متاحة حالياً.", Duration = 3})
                        end
                    end
                end
            end)
        end)
    end
})

OthersTab:CreateButton({
    Name = "🪄 Give Teleport Tool",
    Callback = function()
        local mouse = lp:GetMouse()
        local tpTool = Instance.new("Tool")
        tpTool.Name = "Teleport Tool"
        tpTool.RequiresHandle = false
        tpTool.Parent = lp.Backpack
        
        tpTool.Activated:Connect(function()
            local char = lp.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            if root and mouse.Hit then
                root.CFrame = CFrame.new(mouse.Hit.Position + Vector3.new(0, 3, 0))
            end
        end)
        Rayfield:Notify({Title = "Inventory", Content = "✅ Teleport Tool added to your backpack!", Duration = 3})
    end
})

OthersTab:CreateSection("🛡️ Sky Safezone")
local savedCFrame = nil
local safezonePart = nil

OthersTab:CreateButton({
    Name = "☁️ Teleport to Sky Safezone",
    Callback = function()
        local char = lp.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if root then
            savedCFrame = root.CFrame 
            if not safezonePart then
                safezonePart = Instance.new("Part")
                safezonePart.Name = "InvictusSafezone"
                safezonePart.Size = Vector3.new(50, 5, 50)
                safezonePart.Position = Vector3.new(0, 50000, 0)
                safezonePart.Anchored = true
                safezonePart.Material = Enum.Material.SmoothPlastic
                safezonePart.Color = Color3.fromRGB(20, 20, 20)
                safezonePart.Parent = workspace
            end
            root.CFrame = safezonePart.CFrame + Vector3.new(0, 5, 0)
            Rayfield:Notify({Title = "Safezone", Content = "🛡️ Teleported to Sky Safezone. Location saved!", Duration = 3})
        end
    end
})

OthersTab:CreateButton({
    Name = "🔙 Return From Safezone",
    Callback = function()
        local char = lp.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if root and savedCFrame then
            root.CFrame = savedCFrame
            Rayfield:Notify({Title = "Safezone", Content = "🔙 Returned to your previous location.", Duration = 3})
        else
            Rayfield:Notify({Title = "Safezone", Content = "❌ No saved location to return to!", Duration = 3})
        end
    end
})
 
-- ===============================================

-- ===============================================
-- [ نظام الحركات (Troll / Bang System) ]
-- ===============================================
local RunService = game:GetService("RunService")
local TargetPlayerName = ""
local AttachLoop = nil
local CurrentMode = ""
local BangSpeed = 35
local TimeCounter = 0

OthersTab:CreateSection("🔥 نظام الحركات (Troll)")

-- 1. دالة لجلب أسماء اللاعبين في السيرفر (بدون اسم اللاعب نفسه)
local function GetPlayerNames()
    local playerNames = {}
    local lp = game.Players.LocalPlayer
    for _, player in ipairs(game.Players:GetPlayers()) do
        if player.Name ~= lp.Name then -- لا تضيف اسمك للقائمة
            table.insert(playerNames, player.Name)
        end
    end
    -- إذا كان السيرفر فارغاً (أنت وحدك)
    if #playerNames == 0 then
        table.insert(playerNames, "لا يوجد لاعبون آخرون")
    end
    return playerNames
end

-- 2. قائمة اختيار اسم اللاعب
local PlayerDropdown = OthersTab:CreateDropdown({
    Name = "اختر اللاعب (Target Player)",
    Options = GetPlayerNames(),
    CurrentOption = {"اختر لاعباً..."},
    MultipleOptions = false,
    Flag = "PlayerDropdown", 
    Callback = function(Option)
        -- القائمة تعود بجدول يحتوي الخيار المختار، نأخذ العنصر الأول
        if Option[1] ~= "اختر لاعباً..." and Option[1] ~= "لا يوجد لاعبون آخرون" then
            TargetPlayerName = Option[1]
        else
            TargetPlayerName = ""
        end
    end,
})

-- 3. زر تحديث قائمة اللاعبين
OthersTab:CreateButton({
    Name = "🔄 تحديث قائمة اللاعبين",
    Callback = function()
        PlayerDropdown:Refresh(GetPlayerNames(), {"اختر لاعباً..."})
        Rayfield:Notify({
            Title = "تم التحديث",
            Content = "تم تحديث قائمة اللاعبين في السيرفر.",
            Duration = 2
        })
    end
})

-- 4. مؤشر التحكم بسرعة الحركة
OthersTab:CreateSlider({
    Name = "سرعة الحركة (Speed)",
    Range = {0, 100},
    Increment = 1,
    CurrentValue = 35,
    Flag = "BangSpeedSlider",
    Callback = function(Value)
        BangSpeed = Value
    end,
})

-- الدالة الأساسية لتشغيل الحركات (تقوم بإيقاف الحركة السابقة تلقائياً)
local function StartAttachLoop(mode)
    -- التحقق من اختيار لاعب أولاً
    if TargetPlayerName == "" then
        Rayfield:Notify({
            Title = "خطأ",
            Content = "يرجى اختيار لاعب من القائمة أولاً!",
            Duration = 3
        })
        return
    end

    -- إيقاف أي حركة سابقة إذا كانت تعمل
    if AttachLoop then 
        AttachLoop:Disconnect() 
        AttachLoop = nil 
    end
    
    CurrentMode = mode
    TimeCounter = 0
    
    AttachLoop = RunService.Heartbeat:Connect(function(deltaTime)
        local targetPlayer = game.Players:FindFirstChild(TargetPlayerName)
        local lp = game.Players.LocalPlayer
        
        -- التأكد من أن اللاعب المستهدف لا يزال موجوداً وأن الشخصيات موجودة
        if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
            local targetRoot = targetPlayer.Character.HumanoidRootPart
            local localRoot = lp.Character.HumanoidRootPart
            
            localRoot.Velocity = Vector3.new(0, 0, 0)
            
            -- حساب السرعة
            if BangSpeed > 0 then
                TimeCounter = TimeCounter + (deltaTime * BangSpeed * 2)
            else
                TimeCounter = 0
            end
            
            -- تنفيذ الحركة بناءً على الزر المضغوط
            if CurrentMode == "MOUTH" then
                local bangOffset = math.sin(TimeCounter) * 0.75
                localRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 1.25, -0.4 + bangOffset) * CFrame.Angles(0, math.rad(180), 0)
            elseif CurrentMode == "BACK" then
                local bangOffset = math.sin(TimeCounter) * 0.75
                localRoot.CFrame = targetRoot.CFrame * CFrame.new(0, -0.5, 0.4 - bangOffset)
            elseif CurrentMode == "ROTATE" then
                local radius = 2
                local x = math.cos(TimeCounter) * radius
                local z = math.sin(TimeCounter) * radius
                localRoot.CFrame = CFrame.new(targetRoot.Position + Vector3.new(x, 1, z), targetRoot.Position)
            end
        else
            -- إذا غادر اللاعب السيرفر أثناء تشغيل الحركة
            AttachLoop:Disconnect()
            AttachLoop = nil
            TargetPlayerName = ""
            Rayfield:Notify({
                Title = "تنبيه",
                Content = "لقد غادر اللاعب السيرفر أو ماتت شخصيته.",
                Duration = 3
            })
        end
    end)
end

-- 5. أزرار تشغيل الحركات
OthersTab:CreateButton({
    Name = "Mouth Bang (حركة أمامية)",
    Callback = function() 
        StartAttachLoop("MOUTH") 
    end
})

OthersTab:CreateButton({
    Name = "Back Mode (حركة خلفية)",
    Callback = function() 
        StartAttachLoop("BACK") 
    end
})

OthersTab:CreateButton({
    Name = "Fast Rotation (دوران سريع)",
    Callback = function() 
        StartAttachLoop("ROTATE") 
    end
})

-- 6. زر الإيقاف النهائي
OthersTab:CreateButton({
    Name = "🛑 إيقاف الحركة (Stop Follow)",
    Callback = function()
        if AttachLoop then 
            AttachLoop:Disconnect() 
            AttachLoop = nil 
            Rayfield:Notify({
                Title = "إيقاف",
                Content = "تم إيقاف الحركة بنجاح.",
                Duration = 2
            })
        end
    end
})

-- ===============================================
-- [ 4.5 ] Global Chat System (Enhanced)
-- ===============================================
local DATABASE_URL = "https://invictus-chat-default-rtdb.firebaseio.com/chat.json"
local http_req = (syn and syn.request) or (http and http.request) or http_request or request

if http_req then
    local currentInputText = ""
    local latestTimestamp = 0
    local firstFetch = true

    -- [ Tags & Colors Configuration ]
    local DefaultNameColor = "#CCCCCC" -- Light gray for regular players
    local CustomTags = {
        ["1The_Invictus"] = {Tag = "Owner", Color = "#FF0000"}, -- Owner (Red)
        ["saif_ghzwa"] = {Tag = "Admin", Color = "#00FF00"}, -- Admin (Green)
        ["AstroboyEarth"] = {Tag = "VIP", Color = "#FFD700"}, -- المشكلة كانت هنا (أضفنا الفاصلة في النهاية)
        ["shi_zhenn"] = {Tag = "VIP", Color = "#FFD700"} -- السطر الأخير لا يحتاج لفاصلة
    }


    local ChatDisplay = ChatTab:CreateParagraph({
        Title = "💬 Global Chat (Invictus)",
        Content = "Connecting to server and fetching messages..."
    })

    ChatTab:CreateInput({
        Name = "Type your message",
        PlaceholderText = "Your message here...",
        RemoveTextAfterFocusLost = true,
        Callback = function(Text)
            currentInputText = Text
        end
    })

    -- Play notification sound function
    local function PlayNotificationSound()
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://4562203769" -- Distinct notification sound
        sound.Volume = 2
        sound.Parent = game:GetService("CoreGui")
        sound:Play()
        game:GetService("Debris"):AddItem(sound, 3)
    end

    local function SendChatMessage()
        if currentInputText == "" or string.gsub(currentInputText, "%s+", "") == "" then return end
        
        local messageToSend = currentInputText
        currentInputText = ""

        task.spawn(function()
            pcall(function()
                local payload = HttpService:JSONEncode({
                    sender = lp.DisplayName,
                    username = lp.Name,
                    message = messageToSend,
                    timestamp = os.time()
                })

                http_req({
                    Url = DATABASE_URL,
                    Method = "POST",
                    Headers = { ["Content-Type"] = "application/json" },
                    Body = payload
                })
            end)
        end)
    end

    ChatTab:CreateButton({
        Name = "📤 Send Message",
        Callback = function()
            SendChatMessage()
        end
    })

    local function FetchMessages()
        local fetchUrl = DATABASE_URL .. '?orderBy="$key"&limitToLast=10'
        
        pcall(function()
            local response = http_req({
                Url = fetchUrl,
                Method = "GET"
            })

            if response and response.StatusCode == 200 then
                local data = HttpService:JSONDecode(response.Body)
                if data and type(data) == "table" then
                    local messagesList = {}
                    for key, val in pairs(data) do
                        table.insert(messagesList, val)
                    end
                    
                    table.sort(messagesList, function(a, b)
                        return (a.timestamp or 0) < (b.timestamp or 0)
                    end)

                    local formattedChat = ""
                    local newLatestTimestamp = latestTimestamp

                    for _, msg in ipairs(messagesList) do
                        local msgTime = msg.timestamp or 0
                        local timeStr = os.date("%X", msgTime)
                        local username = msg.username or "Unknown"
                        local displayName = msg.sender or username
                        local textMsg = tostring(msg.message)
                        
                        -- Tags & Colors Processing (RichText)
                        local tagInfo = CustomTags[username]
                        local nameColor = DefaultNameColor
                        local tagStr = ""
                        
                        if tagInfo then
                            tagStr = "<b>[<font color='" .. tagInfo.Color .. "'>" .. tagInfo.Tag .. "</font>]</b> "
                            nameColor = tagInfo.Color
                        end
                        
                        local formattedSender = tagStr .. "<b><font color='" .. nameColor .. "'>" .. displayName .. "</font></b>"
                        
                        formattedChat = formattedChat .. "[" .. timeStr .. "] " .. formattedSender .. ":\n> " .. textMsg .. "\n\n"

                        -- Notifications and sound for new messages
                        if msgTime > newLatestTimestamp then
                            newLatestTimestamp = msgTime
                            
                            -- Make sure the message is new, not from the player themselves, and not on the first fetch
                            if not firstFetch and username ~= lp.Name then
                                PlayNotificationSound()
                                Rayfield:Notify({
                                    Title = "💬 Message from " .. displayName,
                                    Content = textMsg,
                                    Duration = 4
                                })
                            end
                        end
                    end

                    latestTimestamp = newLatestTimestamp
                    firstFetch = false

                    if formattedChat == "" then
                        formattedChat = "No messages available at the moment."
                    end

                    ChatDisplay:Set({
                        Title = "💬 Global Chat (Invictus)",
                        Content = formattedChat
                    })
                end
            end
        end)
    end

    task.spawn(function()
        FetchMessages()
        while task.wait(1) do -- Fastest safe fetch rate (1 second)
            FetchMessages()
        end
    end)
else
    ChatTab:CreateParagraph({
        Title = "❌ System Error",
        Content = "Your Executor does not support external HTTP requests, so the chat will not work."
    })
end

-- ===============================================
-- Settings Tab
-- ===============================================
SettingsTab:CreateSection("Save Management (Settings)")
SettingsTab:CreateButton({
  Name = "💾 Save UI Settings (All)",
  Callback = function()
      pcall(function() Rayfield:SaveConfiguration() end)
      Rayfield:Notify({Title = "Saved!", Content = "All UI toggles and settings saved", Duration = 3})
  end
})
local AntiExplodeNotif = false

-- ضع الزر في التبويب الصحيح (قمت بوضع GrabTab كمثال بناءً على كودك السابق)
SettingsTab:CreateToggle({
    Name = "⚠️ Explosion Tracker",
    CurrentValue = false,
    Flag = "ExplodeTrackerToggle",
    Callback = function(Value)
        AntiExplodeNotif = Value
    end
})
-- ====================================================
-- [ PERFORMANCE & GRAPHICS ENGINE (Integrated) ]
-- ====================================================

local UltraLoop = nil
local Lighting = game:GetService("Lighting")

local function ClearAllShaders()
    if UltraLoop then UltraLoop:Disconnect(); UltraLoop = nil end
    for _, shader in pairs(Lighting:GetChildren()) do
        if shader:IsA("PostEffect") or shader:IsA("BlurEffect") or shader:IsA("SunRaysEffect") or shader:IsA("ColorCorrectionEffect") or shader:IsA("BloomEffect") or shader:IsA("DepthOfFieldEffect") or shader:IsA("Atmosphere") then
            shader:Destroy()
        end
    end
end

-- 1: ANTI-LAG PROFILE CODE
local function NormalAntiLag()
    ClearAllShaders()
    Lighting.GlobalShadows = false
    pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level05 end)
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("ParticleEmitter") or obj:IsA("Sparkles") then obj.Enabled = false end
    end
end

local function MediumAntiLag()
    ClearAllShaders()
    pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level03 end)
    Lighting.GlobalShadows = false
    local Terrain = workspace:FindFirstChildOfClass("Terrain")
    if Terrain then Terrain.WaterWaveSize = 0; Terrain.WaterWaveSpeed = 0 end
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Sparkles") then obj.Enabled = false end
    end
end

local function SuperAntiLag()
    ClearAllShaders()
    pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
    Lighting.GlobalShadows = false
    local Terrain = workspace:FindFirstChildOfClass("Terrain")
    if Terrain then
        Terrain.WaterWaveSize = 0; Terrain.WaterWaveSpeed = 0
        Terrain.WaterReflectance = 0; Terrain.WaterDetailSize = 0
    end
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("BasePart") or obj:IsA("MeshPart") then
            obj.Material = Enum.Material.SmoothPlastic
            obj.Reflectance = 0; obj.CastShadow = false
        elseif obj:IsA("Texture") or obj:IsA("Decal") then
            obj:Destroy()
        elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Sparkles") or obj:IsA("Fire") then
            obj.Enabled = false
        end
    end
end

local function UltraAntiLag()
    SuperAntiLag()
    local function AbsolutePotato(obj)
        if obj:IsA("BasePart") or obj:IsA("MeshPart") then
            obj.Material = Enum.Material.SmoothPlastic
            obj.CastShadow = false
        elseif obj:IsA("Texture") or obj:IsA("Decal") or obj:IsA("Clothing") then
            obj:Destroy()
        elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Sparkles") or obj:IsA("Fire") then
            obj.Enabled = false
        end
    end
    UltraLoop = game.DescendantAdded:Connect(function(newObj)
        pcall(function() AbsolutePotato(newObj) end)
    end)
end

-- 2: REALISTIC SHADERS CODE
local function ApplyRealistic()
    ClearAllShaders()
    Lighting.GlobalShadows = true
    Lighting.Brightness = 2
    Lighting.Ambient = Color3.fromRGB(40, 40, 50)
    local Bloom = Instance.new("BloomEffect", Lighting)
    Bloom.Intensity = 0.4; Bloom.Size = 12; Bloom.Threshold = 0.9
    local ColorCorr = Instance.new("ColorCorrectionEffect", Lighting)
    ColorCorr.Contrast = 0.1; ColorCorr.Saturation = 0.15
end

local function ApplySuperRealistic()
    ClearAllShaders()
    Lighting.GlobalShadows = true
    Lighting.Brightness = 2.5
    local Bloom = Instance.new("BloomEffect", Lighting)
    Bloom.Intensity = 0.6; Bloom.Size = 24; Bloom.Threshold = 0.85
    local SunRays = Instance.new("SunRaysEffect", Lighting)
    SunRays.Intensity = 0.25; SunRays.Spread = 0.6
    local ColorCorr = Instance.new("ColorCorrectionEffect", Lighting)
    ColorCorr.Contrast = 0.2; ColorCorr.Saturation = 0.3
    ColorCorr.TintColor = Color3.fromRGB(255, 245, 230)
end

local function ApplyUltraRealistic()
    ClearAllShaders()
    Lighting.GlobalShadows = true
    Lighting.Brightness = 3
    local Bloom = Instance.new("BloomEffect", Lighting)
    Bloom.Intensity = 0.8; Bloom.Size = 30; Bloom.Threshold = 0.8
    local SunRays = Instance.new("SunRaysEffect", Lighting)
    SunRays.Intensity = 0.4; SunRays.Spread = 0.75
    local ColorCorr = Instance.new("ColorCorrectionEffect", Lighting)
    ColorCorr.Contrast = 0.25; ColorCorr.Saturation = 0.4
    local Atmos = Instance.new("Atmosphere", Lighting)
    Atmos.Density = 0.2; Atmos.Glare = 0.4; Atmos.Haze = 1.5
    Atmos.Color = Color3.fromRGB(180, 200, 220); Atmos.Decay = Color3.fromRGB(120, 140, 160)
    local DOF = Instance.new("DepthOfFieldEffect", Lighting)
    DOF.FarIntensity = 0.2; DOF.FocusDistance = 20; DOF.InFocusRadius = 40; DOF.NearIntensity = 0.05
end

-- ====================================================
-- BUTTONS CONNECTION (Inside SettingsTab)
-- ====================================================

SettingsTab:CreateSection("Lag Optimizer")

SettingsTab:CreateButton({
    Name = "🟢 Normal Anti-Lag",
    Callback = function() 
        NormalAntiLag() 
        Rayfield:Notify({Title = "Invictus v22", Content = "Basic performance tweaks applied.", Duration = 3}) 
    end
})

SettingsTab:CreateButton({
    Name = "🟡 Medium Anti-Lag",
    Callback = function() 
        MediumAntiLag() 
        Rayfield:Notify({Title = "Invictus v22", Content = "Standard shaders disabled.", Duration = 3}) 
    end
})

SettingsTab:CreateButton({
    Name = "🟠 Super Anti-Lag",
    Callback = function() 
        SuperAntiLag() 
        Rayfield:Notify({Title = "Invictus v22", Content = "Textures replaced with smooth plastic.", Duration = 3}) 
    end
})

SettingsTab:CreateButton({
    Name = "🔴 Ultra Anti-Lag (Potato Mode)",
    Callback = function() 
        UltraAntiLag() 
        Rayfield:Notify({Title = "Invictus v22", Content = "Engine forced to minimum + live object wiper active!", Duration = 4}) 
    end
})

SettingsTab:CreateSection("Beautiful Shaders")

SettingsTab:CreateButton({
    Name = "✨ Realistic Shader",
    Callback = function() 
        ApplyRealistic() 
        Rayfield:Notify({Title = "Invictus v22", Content = "Ambient colors and light contrast boosted.", Duration = 3}) 
    end
})

SettingsTab:CreateButton({
    Name = "🔥 Super Realistic Shader",
    Callback = function() 
        ApplySuperRealistic() 
        Rayfield:Notify({Title = "Invictus v22", Content = "Cinematic Sunrays and warm profile loaded!", Duration = 3}) 
    end
})

SettingsTab:CreateButton({
    Name = "👑 Ultra Realistic Shader (Next-Gen)",
    Callback = function() 
        ApplyUltraRealistic() 
        Rayfield:Notify({Title = "Invictus v22", Content = "Next-gen Depth of Field and atmosphere activated!", Duration = 4}) 
    end
})

-- ===============================================
-- [ Independent Explosion Tracker ]
-- ===============================================
task.spawn(function()
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local StarterGui = game:GetService("StarterGui")
    local Workspace = game:GetService("Workspace")

    -- 1. مراقبة الانفجارات القريبة
    Workspace.DescendantAdded:Connect(function(obj)
        if AntiExplodeNotif and obj:IsA("Explosion") then
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                local distance = (obj.Position - char.HumanoidRootPart.Position).Magnitude
                if distance < 15 then 
                    local suspect = "Unknown (Server/GUI Mod)"
                    local creatorTag = obj:FindFirstChild("creator")
                    if creatorTag and creatorTag:IsA("ObjectValue") and creatorTag.Value then
                        suspect = creatorTag.Value.Name
                    end
                    pcall(function()
                        StarterGui:SetCore("SendNotification", {
                            Title = "⚠️ Explosion Target!",
                            Text = "Exploded by: " .. suspect,
                            Duration = 6
                        })
                    end)
                end
            end
        end
    end)

    -- 2. مراقبة الشات لأوامر الأدمن
    local function monitorChat(player)
        player.Chatted:Connect(function(msg)
            if AntiExplodeNotif and string.match(string.lower(msg), "explode") then
                pcall(function()
                    StarterGui:SetCore("SendNotification", {
                        Title = "⚠️ Admin Command!",
                        Text = player.Name .. " is using explode commands!",
                        Duration = 6
                    })
                end)
            end
        end)
    end

    for _, v in pairs(Players:GetPlayers()) do
        if v ~= LocalPlayer then
            monitorChat(v)
        end
    end
    Players.PlayerAdded:Connect(function(player)
        monitorChat(player)
    end)
end)
-- ===============================================

-- ===============================================

-- ===============================================
-- [ 5 ] المحرك الأساسي العام (RunService.Heartbeat)
-- ===============================================
RunService.Heartbeat:Connect(function(dt)
  if IsInvisible and lp.Character then
      local rootPart = lp.Character:FindFirstChild("HumanoidRootPart")
      local humanoid = lp.Character:FindFirstChild("Humanoid")
      if rootPart and humanoid then
          local cf = rootPart.CFrame
          local camOffset = humanoid.CameraOffset
          local hidden = cf * CFrame.new(0, -200000, 0)
          rootPart.CFrame = hidden
          humanoid.CameraOffset = hidden:ToObjectSpace(CFrame.new(cf.Position)).Position
          RunService.RenderStepped:Wait()
          if rootPart.Parent then 
              rootPart.CFrame = cf
              humanoid.CameraOffset = camOffset
          end
      end
  end

  if isSpeedEnabled and lp.Character then
      local hum = lp.Character:FindFirstChildOfClass("Humanoid")
      local root = lp.Character:FindFirstChild("HumanoidRootPart")
      if hum and root then
          hum.WalkSpeed = currentWalkSpeed
          if hum.MoveDirection.Magnitude > 0 then
              root.AssemblyLinearVelocity = Vector3.new(
                  hum.MoveDirection.X * currentWalkSpeed,
                  root.AssemblyLinearVelocity.Y,
                  hum.MoveDirection.Z * currentWalkSpeed
              )
          else
              root.AssemblyLinearVelocity = Vector3.new(
                  0,
                  root.AssemblyLinearVelocity.Y,
                  0
              )
          end
      end
  end

  if AuraEnabled then
      local currentTick = tick()
      if currentTick - lastHitTick >= (1 / apsVal) then
          lastHitTick = currentTick
          local ch = lp.Character
          if ch then
              local myhrp = ch:FindFirstChild("HumanoidRootPart")
              if myhrp then
                  local nearest, nearDist = nil, math.huge
                  for _, p in ipairs(Players:GetPlayers()) do
                      if p ~= lp and p.Character and not isSafe(p.Name) and isTarget(p.Name) then
                          local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                          local hum = p.Character:FindFirstChildOfClass("Humanoid")
                          if hrp and hum and hum.Health > 0 then
                              local dist = (hrp.Position - myhrp.Position).Magnitude
                              if dist <= rangeVal and dist < nearDist then
                                  nearDist = dist; nearest = {hum = hum, pos = hrp.Position + hrp.CFrame.LookVector * 2}
                              end
                          end
                      end
                  end
                  if nearest then task.spawn(function() doHit(nearest.hum, nearest.pos) end) end
              end
          end
      end
  end

  local shouldGrab = AutoGrab
  local activeTargetHRP = nil 

  if AutoTargetEnabled then
      local myChar = lp.Character
      if myChar and myChar:FindFirstChild("HumanoidRootPart") then
          for _, player in ipairs(Players:GetPlayers()) do
              if player ~= lp and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                  if (myChar.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude <= TriggerDistance then
                      shouldGrab = true
                      activeTargetHRP = player.Character.HumanoidRootPart
                      break
                  end
              end
          end
      end
  end
  
  if SpecificTargetEnabled then
      local myChar = lp.Character
      if myChar and myChar:FindFirstChild("HumanoidRootPart") then
          local function checkTarget(targetName)
              if targetName ~= "" and targetName ~= "None" then
                  local p = Players:FindFirstChild(targetName)
                  if p and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                      if (myChar.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude <= TriggerDistance then
                          return p.Character.HumanoidRootPart
                      end
                  end
              end
              return nil
          end

          activeTargetHRP = checkTarget(ChosenTargetName1)
          if not activeTargetHRP then activeTargetHRP = checkTarget(ChosenTargetName2) end
          if not activeTargetHRP then activeTargetHRP = checkTarget(ChosenTargetName3) end
          
          if activeTargetHRP then shouldGrab = shouldGrab or true end
      end
  end
    -- ===============================================
    -- نظام الـ Spinbot المطور (تخطي قفل الكاميرا داخل الحلبة)
    -- ===============================================
    if SpinbotEnabled and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character:FindFirstChild("Humanoid") then
        local myRoot = lp.Character.HumanoidRootPart
        local hum = lp.Character.Humanoid
        
        -- 1. إجبار اللعبة على عدم تدوير الجسد مع الكاميرا
        hum.AutoRotate = false 

        -- 2. إزالة أي BodyGyro سابق لمنع التعارض
        local oldBg = myRoot:FindFirstChild("SpinbotGyro")
        if oldBg then oldBg:Destroy() end

        -- 3. تحديث زاوية الدوران بشكل المستمر
        _G.SpinAngle = ((_G.SpinAngle or 0) + math.rad(SpinbotSpeed * (dt * 60))) % (math.pi * 2)
        
        -- 4. فرض الدوران المباشر على CFrame لتجاوز سكربت الكاميرا الخاص باللعبة
        local currentPos = myRoot.Position
        myRoot.CFrame = CFrame.new(currentPos) * CFrame.Angles(0, _G.SpinAngle, 0)
    else
        -- إعادة وضع الدوران إلى الطبيعي عند إيقاف السكربت
        if lp.Character and lp.Character:FindFirstChild("Humanoid") then
            lp.Character.Humanoid.AutoRotate = true
        end
    end


  -- دمج الأعداء اليدويين كأهداف استهداف للمغناطيس أو القفل
  local myChar = lp.Character
  if myChar and myChar:FindFirstChild("HumanoidRootPart") then
      local myRoot = myChar.HumanoidRootPart
      local currentGrabPower = lp:GetAttribute("GrabPower") or 0
      local isFighting = lp:GetAttribute("Fighting")

      -- البحث عن أول عدو يدوي قريب ضمن المدى
      for _, p in ipairs(Players:GetPlayers()) do
          if p ~= lp and p.Character and p.Character:FindFirstChild("HumanoidRootPart") and isSelectedEnemy(p.Name) then
              local dist = (myRoot.Position - p.Character.HumanoidRootPart.Position).Magnitude
              if dist <= TriggerDistance then
                  activeTargetHRP = p.Character.HumanoidRootPart
                  shouldGrab = true
                  break
              end
          end
      end
      
if activeTargetHRP then
          if not isFighting then
              if AutoLookEnabled then
                  local targetPos = Vector3.new(activeTargetHRP.Position.X, myRoot.Position.Y, activeTargetHRP.Position.Z)
                  if (targetPos - myRoot.Position).Magnitude > 0.5 then
                      local bg = myRoot:FindFirstChild("AutoLookGyro")
                      if not bg then
                          bg = Instance.new("BodyGyro")
                          bg.Name = "AutoLookGyro"
                          bg.MaxTorque = Vector3.new(0, 400000, 0)
                          bg.P = 30000
                          bg.D = 100
                          bg.Parent = myRoot
                      end
                      bg.CFrame = CFrame.lookAt(myRoot.Position, targetPos)
                  else
                      removeLookGyro()
                  end
              else
                  removeLookGyro()
              end
              
              if AutoMagnetEnabled and currentGrabPower >= 100 then
                  local targetVelocity = activeTargetHRP.AssemblyLinearVelocity
                  local pingPrediction = targetVelocity * 0.05
                  myRoot.CFrame = (activeTargetHRP.CFrame + pingPrediction) * CFrame.new(0, 0, 2.5)
                  
                  if firetouchinterest then
                      firetouchinterest(myRoot, activeTargetHRP, 0)
                      task.wait()
                      firetouchinterest(myRoot, activeTargetHRP, 1)
                  end
              end
          else
              removeLookGyro()
          end

          if IsHoldingMagnet and currentGrabPower >= 100 then
              local targetVelocity = activeTargetHRP.AssemblyLinearVelocity
              local pingPrediction = targetVelocity * 0.05
              myRoot.CFrame = (activeTargetHRP.CFrame + pingPrediction) * CFrame.new(0, 0, 2.5)
              
              if firetouchinterest then
                  firetouchinterest(myRoot, activeTargetHRP, 0)
                  task.wait()
                  firetouchinterest(myRoot, activeTargetHRP, 1)
              end
          end
      else
          removeLookGyro()
      end
  end

  if shouldGrab and GrabRF then
      for i = 1, grabRepeatVal do 
          task.spawn(function() 
              pcall(function() GrabRF:InvokeServer() end) 
          end) 
      end
  end
end)

pcall(function()
  Rayfield:LoadConfiguration()
end)
