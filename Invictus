-- ===============================================
-- [ نظام حماية التشغيل المحسن لـ Delta ]
-- ===============================================
repeat task.wait() until game:IsLoaded()
task.wait(0.5)

pcall(function()
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "نظام التشغيل",
        Text = "جاري تحميل واجهة KHURAFI...",
        Duration = 3
    })
end)

local success, Rayfield = pcall(function()
    return loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
end)

if not success or type(Rayfield) ~= "table" then
    success, Rayfield = pcall(function()
        return loadstring(game:HttpGet('https://raw.githubusercontent.com/sirius-xyz/Rayfield/main/source.lua'))()
    end)
end

if not success or type(Rayfield) ~= "table" then
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "خطأ",
            Text = "فشل تحميل الواجهة، تأكد من الإنترنت.",
            Duration = 5
        })
    end)
    return 
end

-- ===============================================
-- استدعاء الخدمات الأساسية
-- ===============================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local lp = Players.LocalPlayer

-- ===============================================
-- [ 1 ] إعدادات البيانات
-- ===============================================
local DATA_FILE = "aurakill_delta_data_v4.json"
local SAVE = {safe1 = "", safe2 = "", safe3 = ""}

local function loadData()
    pcall(function()
        if writefile and isfile and isfile(DATA_FILE) then
            local data = HttpService:JSONDecode(readfile(DATA_FILE))
            if data and type(data) == "table" then
                SAVE.safe1 = data.safe1 or ""
                SAVE.safe2 = data.safe2 or ""
                SAVE.safe3 = data.safe3 or ""
            end
        end
    end)
end

local function saveData()
    pcall(function()
        if writefile then writefile(DATA_FILE, HttpService:JSONEncode(SAVE)) end
    end)
end
loadData()

local function isSafe(name)
    local lowerName = name:lower()
    local boxes = {SAVE.safe1, SAVE.safe2, SAVE.safe3}
    for _, boxText in ipairs(boxes) do
        for s in boxText:gmatch("%S+") do
            if lowerName:find(s:lower(), 1, true) then return true end
        end
    end
    return false
end

local function getPlayerNames(includeAll)
    local names = {}
    if includeAll then table.insert(names, "All") end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= lp then table.insert(names, p.Name) end
    end
    return names
end

-- ===============================================
-- [ 2 ] إعدادات الأورا والسحب
-- ===============================================
local HitR = nil
local function findHitRemote()
    local function search(obj)
        for _, child in pairs(obj:GetChildren()) do
            if (child:IsA("RemoteFunction") or child:IsA("RemoteEvent")) and child.Name == "Hit" then return child end
            local found = search(child)
            if found then return found end
        end
        return nil
    end
    HitR = search(ReplicatedStorage)
    if not HitR then
        local combat = ReplicatedStorage:FindFirstChild("Packages")
        if combat then combat = combat:FindFirstChild("Knit") end
        if combat then combat = combat:FindFirstChild("Services") end
        if combat then combat = combat:FindFirstChild("CombatService") end
        if combat then
            local rf = combat:FindFirstChild("RF")
            if rf then HitR = rf:FindFirstChild("Hit") end
        end
    end
end
findHitRemote()

local GrabRF = ReplicatedStorage:FindFirstChild("OldPackages") and ReplicatedStorage.OldPackages.Knit.Services.CombatService.RF.Grab

local AuraEnabled = false
local rangeVal = 50
local apsVal = 15
local AuraTargetSelected = "All"
local lastHitTick = 0

local AutoGrab = false
local AutoTargetEnabled = false
local SpecificTargetEnabled = false
local ChosenTargetPlayer1 = nil
local ChosenTargetPlayer2 = nil
local TriggerDistance = 15
local TpSelectedPlayer = nil

local function isTarget(name)
    if AuraTargetSelected == "All" or AuraTargetSelected == "" then return true end
    return name:lower() == AuraTargetSelected:lower()
end

local function doHit(hum, pos)
    if not hum or not hum.Parent then return end
    if HitR then
        if HitR:IsA("RemoteEvent") then HitR:FireServer(hum, Vector3.new(pos.X, pos.Y, pos.Z))
        elseif HitR:IsA("RemoteFunction") then HitR:InvokeServer(hum, Vector3.new(pos.X, pos.Y, pos.Z)) end
    end
end

-- ===============================================
-- [ 3 ] إعدادات تأثيرات الاسم
-- ===============================================
local BWNameActive = false
local currentNameMode = "NONE" 
local rnHue = 0
local bwLastUpdate = 0
local rnSpeed = 0.7        
local rnUpdateRate = 0.02   
local policeToggle = false  
local rainbowNameRemote = ReplicatedStorage:WaitForChild("Remotes", 10)
rainbowNameRemote = rainbowNameRemote and rainbowNameRemote:FindFirstChild("SetRPNameColor")

-- ===============================================
-- [ 4 ] إعدادات ESP
-- ===============================================
local EspEnabled = false
local EspBoxes = true
local EspNames = true

local function removeEsp(char)
    if not char then return end
    if char:FindFirstChild("ESP_Highlight") then char.ESP_Highlight:Destroy() end
    if char:FindFirstChild("ESP_Billboard") then char.ESP_Billboard:Destroy() end
end

local function applyEsp(player)
    if player == lp or not player.Character then return end
    local char = player.Character
    removeEsp(char)
    if not EspEnabled then return end

    if EspBoxes then
        local highlight = Instance.new("Highlight")
        highlight.Name = "ESP_Highlight"
        highlight.Adornee = char
        highlight.FillColor = isSafe(player.Name) and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 50, 50)
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.FillTransparency = 0.5
        highlight.OutlineTransparency = 0
        highlight.Parent = char
    end

    local head = char:FindFirstChild("Head")
    if head and EspNames then
        local billboard = Instance.new("BillboardGui")
        billboard.Name = "ESP_Billboard"
        billboard.Adornee = head
        billboard.Size = UDim2.new(0, 120, 0, 40)
        billboard.StudsOffset = Vector3.new(0, 2.5, 0)
        billboard.AlwaysOnTop = true

        local textLabel = Instance.new("TextLabel")
        textLabel.Size = UDim2.new(1, 0, 1, 0)
        textLabel.BackgroundTransparency = 1
        textLabel.TextColor3 = isSafe(player.Name) and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 255, 255)
        textLabel.TextStrokeTransparency = 0
        textLabel.Font = Enum.Font.SourceSansBold
        textLabel.TextSize = 13
        textLabel.Parent = billboard
        billboard.Parent = char

        task.spawn(function()
            while EspEnabled and char and char.Parent and head and head.Parent do
                local myChar = lp.Character
                if myChar and myChar:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("HumanoidRootPart") then
                    local dist = math.floor((myChar.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude)
                    textLabel.Text = player.Name .. "\n[" .. tostring(dist) .. " Studs]"
                end
                task.wait(0.2)
            end
        end)
    end
end

local function refreshAllEsp()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= lp and p.Character then
            if EspEnabled then applyEsp(p) else removeEsp(p.Character) end
        end
    end
end

for _, p in ipairs(Players:GetPlayers()) do 
    if p ~= lp then p.CharacterAdded:Connect(function() if EspEnabled then task.wait(0.5) applyEsp(p) end end) end
end
Players.PlayerAdded:Connect(function(plr) 
    if plr ~= lp then plr.CharacterAdded:Connect(function() if EspEnabled then task.wait(0.5) applyEsp(plr) end end) end
end)

-- ===============================================
-- [ 5 ] نظام التخفي والواجهة العائمة
-- ===============================================
local IsInvisible = false
local invisParts = {}

local InvisGui = Instance.new("ScreenGui")
InvisGui.Name = "FloatingInvisUI"
InvisGui.ResetOnSpawn = false
local successGui = pcall(function() InvisGui.Parent = CoreGui end)
if not successGui then InvisGui.Parent = lp:WaitForChild("PlayerGui") end
InvisGui.Enabled = false 

local FloatBtn = Instance.new("TextButton")
FloatBtn.Size = UDim2.new(0, 100, 0, 45)
FloatBtn.Position = UDim2.new(0.5, -50, 0.1, 0)
FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
FloatBtn.Text = "تخفي: ❌"
FloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FloatBtn.Font = Enum.Font.SourceSansBold
FloatBtn.TextSize = 18
FloatBtn.Active = true
FloatBtn.Draggable = true 
FloatBtn.Parent = InvisGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = FloatBtn

local function updateInvisParts()
    invisParts = {}
    if lp.Character then
        for _, obj in pairs(lp.Character:GetDescendants()) do
            if (obj:IsA("BasePart") or obj:IsA("Decal")) and obj.Name ~= "HumanoidRootPart" then
                if obj.Transparency < 1 then
                    table.insert(invisParts, {obj = obj, origTrans = obj.Transparency})
                end
            end
        end
    end
end

FloatBtn.MouseButton1Click:Connect(function()
    IsInvisible = not IsInvisible
    if IsInvisible then
        FloatBtn.BackgroundColor3 = Color3.fromRGB(60, 220, 60)
        FloatBtn.Text = "تخفي: ✅"
        updateInvisParts()
        for _, item in pairs(invisParts) do
            if item.obj and item.obj.Parent then item.obj.Transparency = 0.5 end
        end
    else
        FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
        FloatBtn.Text = "تخفي: ❌"
        for _, item in pairs(invisParts) do
            if item.obj and item.obj.Parent then item.obj.Transparency = item.origTrans end
        end
    end
end)

lp.CharacterAdded:Connect(function()
    IsInvisible = false
    FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
    FloatBtn.Text = "تخفي: ❌"
end)

-- ===============================================
-- [ 6 ] إعدادات اللاعب (السرعة والقفز)
-- ===============================================
local isInfJump = false
local isSpeedEnabled = false
local currentWalkSpeed = 35

UserInputService.JumpRequest:Connect(function()
    if isInfJump and lp.Character then
        local humanoid = lp.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end
    end
end)

-- ===============================================
-- [ 7 ] بناء واجهة Rayfield
-- ===============================================
local Window = Rayfield:CreateWindow({
   Name = "الأنفكتوس العراقي",
   LoadingTitle = "Aura, AINZ, ESP & More",
   LoadingSubtitle = "by KHURAFI",
   Theme = "Default",
   DisableRayfieldPrompts = false,
   DisableBuildWarnings = true,
   ConfigurationSaving = { Enabled = false },
   KeySystem = false
})

local MainTab = Window:CreateTab("Aura", 4483345998)
local PlayerTab = Window:CreateTab("اللاعب", 4483345998)
local EspTab = Window:CreateTab("ESP", 4483345998)
local AinzTab = Window:CreateTab("AINZ", 4483345998)
local NameTab = Window:CreateTab("الاسم", 4483345998)
local SettingsTab = Window:CreateTab("الإعدادات", 4483345998)

-- Aura
MainTab:CreateToggle({ Name = "تفعيل الأورا (STATUS)", CurrentValue = false, Flag = "AuraToggle", Callback = function(Value) AuraEnabled = Value end })
MainTab:CreateSlider({ Name = "نطاق الضرر", Range = {5, 100}, Increment = 1, Suffix = "Studs", CurrentValue = 50, Flag = "RangeSlider", Callback = function(Value) rangeVal = Value end })
MainTab:CreateSlider({ Name = "سرعة الضرب", Range = {1, 60}, Increment = 1, Suffix = "Hit/s", CurrentValue = 15, Flag = "APSSlider", Callback = function(Value) apsVal = Value end })
local AuraTargetDropdown = MainTab:CreateDropdown({ Name = "الهدف", Options = getPlayerNames(true), CurrentOption = {"All"}, MultipleOptions = false, Flag = "AuraTargetDropdown", Callback = function(Options) if Options[1] then AuraTargetSelected = Options[1] end end })
MainTab:CreateButton({ Name = "تحديث الأهداف", Callback = function() AuraTargetDropdown:Refresh(getPlayerNames(true)) end })

-- Player
PlayerTab:CreateSection("إعدادات التخفي")
PlayerTab:CreateToggle({ 
    Name = "إظهار زر التخفي العائم", 
    CurrentValue = false, 
    Flag = "InvisSystemToggle", 
    Callback = function(Value) 
        InvisGui.Enabled = Value 
        if not Value and IsInvisible then
            IsInvisible = false
            FloatBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
            FloatBtn.Text = "تخفي: ❌"
            for _, item in pairs(invisParts) do
                if item.obj and item.obj.Parent then item.obj.Transparency = item.origTrans end
            end
        end
    end 
})

PlayerTab:CreateSection("السرعة والقفز")
PlayerTab:CreateToggle({ 
    Name = "تثبيت السرعة (مضاد للتبطئ)", 
    CurrentValue = false, 
    Flag = "SpeedToggle", 
    Callback = function(Value) 
        isSpeedEnabled = Value 
        if not Value and lp.Character then
            local hum = lp.Character:FindFirstChildOfClass("Humanoid")
            if hum then hum.WalkSpeed = 16 end
        end
    end 
})
PlayerTab:CreateSlider({ Name = "قيمة السرعة", Range = {16, 150}, Increment = 1, Suffix = "WS", CurrentValue = 35, Flag = "SpeedSlider", Callback = function(Value) currentWalkSpeed = Value end })
PlayerTab:CreateToggle({ Name = "قفز لانهائي", CurrentValue = false, Flag = "InfJumpToggle", Callback = function(Value) isInfJump = Value end })

-- ESP
EspTab:CreateToggle({ Name = "تفعيل ESP", CurrentValue = false, Flag = "EspMainToggle", Callback = function(Value) EspEnabled = Value; refreshAllEsp() end })
EspTab:CreateToggle({ Name = "تظليل (Highlight)", CurrentValue = true, Flag = "EspBoxesToggle", Callback = function(Value) EspBoxes = Value; refreshAllEsp() end })
EspTab:CreateToggle({ Name = "أسماء ومسافة", CurrentValue = true, Flag = "EspNamesToggle", Callback = function(Value) EspNames = Value; refreshAllEsp() end })

-- AINZ (تم تحديث قسم الإمساك المخصص ليشمل هدفين)
AinzTab:CreateToggle({ Name = "سحب مستمر", CurrentValue = false, Flag = "AutoGrabToggle", Callback = function(Value) AutoGrab = Value end })
AinzTab:CreateToggle({ Name = "استهداف تلقائي للكل", CurrentValue = false, Flag = "AutoTargetToggle", Callback = function(Value) AutoTargetEnabled = Value end })

AinzTab:CreateSection("الإمساك المخصص (لاعبين محددين)")
local TargetDropdown1 = AinzTab:CreateDropdown({ Name = "اختر اللاعب المستهدف الأول (1)", Options = getPlayerNames(false), CurrentOption = {""}, MultipleOptions = false, Flag = "SpecificTargetDropdown1", Callback = function(Options) if Options[1] and Options[1] ~= "" then ChosenTargetPlayer1 = Players:FindFirstChild(Options[1]) end end })
local TargetDropdown2 = AinzTab:CreateDropdown({ Name = "اختر اللاعب المستهدف الثاني (2)", Options = getPlayerNames(false), CurrentOption = {""}, MultipleOptions = false, Flag = "SpecificTargetDropdown2", Callback = function(Options) if Options[1] and Options[1] ~= "" then ChosenTargetPlayer2 = Players:FindFirstChild(Options[1]) end end })
AinzTab:CreateToggle({ Name = "تفعيل السحب على اللاعبين المحددين", CurrentValue = false, Flag = "SpecificTargetToggle", Callback = function(Value) SpecificTargetEnabled = Value end })

AinzTab:CreateSection("الانتقال السريع")
local TpDropdown = AinzTab:CreateDropdown({ Name = "انتقال إلى لاعب", Options = getPlayerNames(false), CurrentOption = {""}, MultipleOptions = false, Flag = "TpDropdown", Callback = function(Options) TpSelectedPlayer = Options[1] end })
AinzTab:CreateButton({ Name = "انتقال (Teleport)", Callback = function() if TpSelectedPlayer then local targetPlr = Players:FindFirstChild(TpSelectedPlayer) local myChar = lp.Character if targetPlr and targetPlr.Character and targetPlr.Character:FindFirstChild("HumanoidRootPart") and myChar and myChar:FindFirstChild("HumanoidRootPart") then myChar.HumanoidRootPart.CFrame = targetPlr.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 0.8) end end end })
AinzTab:CreateButton({ Name = "تحديث قوائم اللاعبين", Callback = function() TargetDropdown1:Refresh(getPlayerNames(false)); TargetDropdown2:Refresh(getPlayerNames(false)); TpDropdown:Refresh(getPlayerNames(false)) end })

-- Names
NameTab:CreateToggle({ Name = "تفعيل التأثيرات", CurrentValue = false, Flag = "NameEffectToggle", Callback = function(Value) BWNameActive = Value end })
NameTab:CreateDropdown({ Name = "النوع", Options = {"BW", "Rainbow", "Pulse", "Strobe", "Police"}, CurrentOption = {"Rainbow"}, MultipleOptions = false, Flag = "NameEffectModeDropdown", Callback = function(Options) currentNameMode = Options[1] end })
NameTab:CreateSlider({ Name = "السرعة", Range = {0.1, 3.0}, Increment = 0.1, Suffix = "x", CurrentValue = 0.7, Flag = "NameSpeedSlider", Callback = function(Value) rnSpeed = Value end })

-- Settings
SettingsTab:CreateInput({ Name = "SAFE 1", PlaceholderText = "اسم اللاعب 1...", RemoveTextAfterFocusLost = false, CurrentValue = SAVE.safe1, Callback = function(Text) SAVE.safe1 = Text; saveData(); refreshAllEsp() end })
SettingsTab:CreateInput({ Name = "SAFE 2", PlaceholderText = "اسم اللاعب 2...", RemoveTextAfterFocusLost = false, CurrentValue = SAVE.safe2, Callback = function(Text) SAVE.safe2 = Text; saveData(); refreshAllEsp() end })
SettingsTab:CreateInput({ Name = "SAFE 3", PlaceholderText = "اسم اللاعب 3...", RemoveTextAfterFocusLost = false, CurrentValue = SAVE.safe3, Callback = function(Text) SAVE.safe3 = Text; saveData(); refreshAllEsp() end })
SettingsTab:CreateButton({ Name = "حفظ البيانات", Callback = function() saveData(); Rayfield:Notify({Title = "تم الحفظ", Content = "تم بنجاح", Duration = 3}) end })

-- ===============================================
-- [ 8 ] المحرك الأساسي (يعمل لكل شيء)
-- ===============================================
RunService.Heartbeat:Connect(function(dt)
    -- 0. نظام التخفي
    if IsInvisible and lp.Character then
        local rootPart = lp.Character:FindFirstChild("HumanoidRootPart")
        local humanoid = lp.Character:FindFirstChild("Humanoid")
        if rootPart and humanoid then
            local cf = rootPart.CFrame
            local camOffset = humanoid.CameraOffset
            local hidden = cf * CFrame.new(0, -200000, 0)
            rootPart.CFrame = hidden
            humanoid.CameraOffset = hidden:ToObjectSpace(CFrame.new(cf.Position)).Position
            RunService.RenderStepped:Wait()
            if rootPart.Parent then 
                rootPart.CFrame = cf
                humanoid.CameraOffset = camOffset
            end
        end
    end

    -- 1. تثبيت السرعة
    if isSpeedEnabled and lp.Character then
        local hum = lp.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum.WalkSpeed ~= currentWalkSpeed then
            hum.WalkSpeed = currentWalkSpeed
        end
    end

    -- 2. الأورا
    if AuraEnabled then
        local currentTick = tick()
        if currentTick - lastHitTick >= (1 / apsVal) then
            lastHitTick = currentTick
            local ch = lp.Character
            if ch then
                local myhrp = ch:FindFirstChild("HumanoidRootPart")
                if myhrp then
                    local nearest, nearDist = nil, math.huge
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p ~= lp and p.Character and not isSafe(p.Name) and isTarget(p.Name) then
                            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
                            local hum = p.Character:FindFirstChildOfClass("Humanoid")
                            if hrp and hum and hum.Health > 0 then
                                local dist = (hrp.Position - myhrp.Position).Magnitude
                                if dist <= rangeVal and dist < nearDist then
                                    nearDist = dist
                                    nearest = {hum = hum, pos = hrp.Position + hrp.CFrame.LookVector * 2}
                                end
                            end
                        end
                    end
                    if nearest then task.spawn(function() doHit(nearest.hum, nearest.pos) end) end
                end
            end
        end
    end

    -- 3. سحب AINZ (محدث لفحص الهدفين المحددين)
    local shouldGrab = AutoGrab
    if AutoTargetEnabled then
        local myChar = lp.Character
        if myChar and myChar:FindFirstChild("HumanoidRootPart") then
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= lp and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    if (myChar.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude <= TriggerDistance then
                        shouldGrab = true; break
                    end
                end
            end
        end
    end

    if SpecificTargetEnabled then
        local myChar = lp.Character
        if myChar and myChar:FindFirstChild("HumanoidRootPart") then
            -- فحص الهدف الأول
            if ChosenTargetPlayer1 and ChosenTargetPlayer1.Parent and ChosenTargetPlayer1.Character and ChosenTargetPlayer1.Character:FindFirstChild("HumanoidRootPart") then
                if (myChar.HumanoidRootPart.Position - ChosenTargetPlayer1.Character.HumanoidRootPart.Position).Magnitude <= TriggerDistance then
                    shouldGrab = true
                end
            else
                if ChosenTargetPlayer1 and not ChosenTargetPlayer1.Parent then ChosenTargetPlayer1 = nil end
            end

            -- فحص الهدف الثاني
            if ChosenTargetPlayer2 and ChosenTargetPlayer2.Parent and ChosenTargetPlayer2.Character and ChosenTargetPlayer2.Character:FindFirstChild("HumanoidRootPart") then
                if (myChar.HumanoidRootPart.Position - ChosenTargetPlayer2.Character.HumanoidRootPart.Position).Magnitude <= TriggerDistance then
                    shouldGrab = true
                end
            else
                if ChosenTargetPlayer2 and not ChosenTargetPlayer2.Parent then ChosenTargetPlayer2 = nil end
            end
        end
    end

    if shouldGrab and GrabRF then
        for i = 1, 5 do task.spawn(function() pcall(function() GrabRF:InvokeServer() end) end) end
    end

    -- 4. تأثيرات الاسم
    if rainbowNameRemote and BWNameActive then
        local now = os.clock()
        rnHue = (rnHue + (dt * rnSpeed)) % 1
        if now - bwLastUpdate >= (currentNameMode == "Police" and 0.12 or rnUpdateRate) then
            local targetColor
            if currentNameMode == "BW" then local intensity = (math.sin(rnHue * math.pi * 2) + 1) / 2; targetColor = Color3.new(intensity, intensity, intensity)
            elseif currentNameMode == "Rainbow" then targetColor = Color3.fromHSV(rnHue, 1, 1)
            elseif currentNameMode == "Pulse" then targetColor = Color3.fromHSV(rnHue, 0.8, 1)
            elseif currentNameMode == "Strobe" then targetColor = Color3.fromRGB(math.random(50,255), math.random(50,255), math.random(50,255))
            elseif currentNameMode == "Police" then policeToggle = not policeToggle; targetColor = policeToggle and Color3.fromRGB(0, 100, 255) or Color3.fromRGB(255, 0, 0) end
            if targetColor then pcall(function() rainbowNameRemote:FireServer(targetColor) end) end
            bwLastUpdate = now
        end
    end
end)
